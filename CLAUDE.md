# Пульсар - Платформа для жилищных комплексов

## 📋 Описание проекта

**Пульсар** ("В ритме твоего пространства") - это multi-tenant мобильная платформа для жилищных комплексов (ЖК), которая объединяет marketplace, социальные функции и управление сообществом в одном приложении.

**Проблема**: В большом чате посёлка/ЖК теряются объявления о продаже товаров, предложениях услуг, важных новостях и документах.

**Решение**: Структурированное мобильное приложение с концепцией **Пространств** - изолированных сообществ с собственными объявлениями, новостями, документацией и участниками.

---

## ⚡ TL;DR (быстрый контекст)
- **Миссия**: Пульсар — multi-tenant платформа для ЖК. Backend (Ktor + Exposed + PostgreSQL/H2), мобильный клиент на Compose/KMP.
- **Состояние**: Этап 18 в работе — мобильный клиент получил `SpaceManager`, экран SpaceSelection и маршрутизацию Root→SpaceSelection→Main; все product/service API-вызовы теперь прокидывают `spaceId`.
- **Проблемы/ограничения**: Gradle не может писать в `~/.gradle`, поэтому тесты/запуск сервера временно вручную не проверены; нужно починить окружение перед релизом.
- **Следующий шаг**: Завершить Этап 18 — дополировать UX (отображение текущего пространства, переходы в профиле), затем перейти к интеграции spaces в основной UI (Этап 19).

## 🚀 Новая концепция: ПРОСТРАНСТВА (Spaces)

**Дата введения**: 2025-11-16
**Статус**: 🔴 КРИТИЧНОЕ АРХИТЕКТУРНОЕ ИЗМЕНЕНИЕ (до production)

### Что такое Пространство?

**Пространство (Space)** - это изолированный контейнер для жилищного комплекса со своими:
- 📦 **Объявлениями** (товары и услуги)
- 📰 **Новостной лентой** (объявления администрации, события)
- 📄 **Документацией** (устав, правила, договоры)
- 👥 **Участниками** с ролями (OWNER, ADMIN, MEMBER)
- 🔒 **Приватностью** (публичное или приватное пространство)

### Зачем нужна эта концепция?

#### 1. **Multi-tenancy (изоляция данных)**
- Каждый ЖК видит **только свои** объявления и новости
- Невозможно случайно увидеть данные из другого ЖК
- Полная изоляция на уровне БД (через `spaceId`)

**Пример:**
```
ЖК "Солнечный" (spaceId=1):
  - Объявления: 150 товаров, 30 услуг
  - Участники: 500 пользователей
  - Новости: 25 постов

ЖК "Лесной" (spaceId=2):
  - Объявления: 80 товаров, 15 услуг
  - Участники: 300 пользователей
  - Новости: 10 постов
```

#### 2. **Масштабируемость**
- Одно приложение → **тысячи ЖК**
- Не нужно создавать отдельное приложение для каждого ЖК
- Легко добавить новый ЖК без деплоя

#### 3. **Роли и управление**
```kotlin
enum class SpaceMemberRole {
    OWNER,   // Создатель (полный контроль, удаление пространства)
    ADMIN,   // Администратор (модерация, новости, управление участниками)
    MEMBER   // Участник (просмотр, создание объявлений)
}
```

**Права доступа:**
- **OWNER**: управление настройками, удаление пространства, назначение админов
- **ADMIN**: публикация новостей, модерация объявлений, управление документами, бан пользователей
- **MEMBER**: просмотр контента, создание своих объявлений, участие в чатах

#### 4. **Приватность**
```kotlin
enum class SpaceType {
    PUBLIC,   // Любой может найти и присоединиться
    PRIVATE   // Нужен код приглашения (inviteCode)
}
```

- **PUBLIC**: открытые ЖК (список виден всем)
- **PRIVATE**: закрытые ЖК (только по коду приглашения)

### Архитектура Пространств

#### Модель данных (упрощенно):
```
Spaces (пространства)
├─ id: Long
├─ name: String (например, "ЖК Солнечный")
├─ slug: String (например, "jk-solnechnyj")
├─ type: SpaceType (PUBLIC/PRIVATE)
├─ inviteCode: String? (для приватных)
└─ ownerId: Long (создатель)

SpaceMembers (участники)
├─ spaceId: Long (FK → Spaces)
├─ userId: Long (FK → Users)
├─ role: SpaceMemberRole (OWNER/ADMIN/MEMBER)
└─ joinedAt: Instant

Products (товары)
├─ spaceId: Long (FK → Spaces) ✨ NEW!
└─ ... (остальные поля)

Services (услуги)
├─ spaceId: Long (FK → Spaces) ✨ NEW!
└─ ... (остальные поля)

SpaceNews (новости) ✨ NEW!
├─ spaceId: Long (FK → Spaces)
└─ ... (title, content, images)

SpaceDocuments (документы) ✨ NEW!
├─ spaceId: Long (FK → Spaces)
└─ ... (title, fileUrl, fileType)
```

#### Почему нужна таблица SpaceMembers?

**SpaceMembers** - это связующая таблица Many-to-Many между пользователями и пространствами.

**Зачем:**
1. **Один пользователь → несколько пространств**
   Пример: Иван живет в ЖК "Солнечный", но подписан на новости ЖК "Лесной"

2. **Одно пространство → множество участников**
   Пример: ЖК "Солнечный" имеет 500 участников

3. **Роли и права доступа**
   Каждый участник имеет свою роль (OWNER/ADMIN/MEMBER) в каждом пространстве

4. **Проверка доступа**
   Перед показом объявлений: `SELECT * FROM SpaceMembers WHERE spaceId = ? AND userId = ?`

5. **Метаданные**
   Дата вступления (`joinedAt`), история, статистика

**Без SpaceMembers:**
- ❌ Невозможно реализовать роли
- ❌ Невозможно реализовать приватные пространства
- ❌ Плохая производительность (JSON массивы участников)
- ❌ Нельзя отследить, когда пользователь присоединился

### User Journey с Пространствами

**1. Новый пользователь:**
```
Регистрация → SpaceSelectionScreen → Выбор:
  ├─ Присоединиться к существующему (PUBLIC)
  ├─ Присоединиться по коду (PRIVATE)
  └─ Создать новое пространство
```

**2. Работа с объявлениями:**
```
MainScreen (текущее пространство: ЖК "Солнечный")
├─ Товары (фильтр: spaceId = 1)
├─ Услуги (фильтр: spaceId = 1)
├─ Новости (фильтр: spaceId = 1)
└─ Профиль → Переключить пространство
```

**3. Переключение между пространствами:**
```
SpaceSwitcherScreen:
  ✓ ЖК "Солнечный" (текущее)
  ○ ЖК "Лесной"
  ○ ЖК "Парковый"
```

### Миграция существующих данных

**Проблема:** Сейчас все товары и услуги НЕ привязаны к пространству.

**Решение:**
1. Создать "Demo Space" с `id = 1`
2. Добавить поле `spaceId` в `Products` и `Services` (NOT NULL, FK → Spaces)
3. Установить всем существующим товарам/услугам `spaceId = 1`
4. Добавить всех существующих пользователей в "Demo Space" как MEMBER

**SQL (упрощенно):**
```sql
-- Создать Demo Space
INSERT INTO Spaces (name, slug, type, ownerId)
VALUES ('Demo Space', 'demo-space', 'PUBLIC', 1);

-- Добавить spaceId в Products
ALTER TABLE Products ADD COLUMN spaceId BIGINT NOT NULL DEFAULT 1;
ALTER TABLE Products ADD FOREIGN KEY (spaceId) REFERENCES Spaces(id);

-- Добавить всех пользователей в Demo Space
INSERT INTO SpaceMembers (spaceId, userId, role, joinedAt)
SELECT 1, id, 'MEMBER', NOW() FROM Users;
```

### Почему это КРИТИЧНО сделать СЕЙЧАС?

🔴 **ДО запуска в production:**
- ✅ Миграция данных простая (1 пространство)
- ✅ Нет реальных пользователей
- ✅ Можно изменить API без breaking changes

❌ **ПОСЛЕ запуска в production:**
- ❌ Миграция сложная (тысячи объявлений)
- ❌ Нужно уведомлять пользователей
- ❌ Breaking changes в API
- ❌ Риск потери данных

### Технический стек изменений

**Backend:**
- ✨ Новые таблицы: `Spaces`, `SpaceMembers`, `SpaceNews`, `SpaceDocuments`, `Conversations`, `Messages`
- 🔄 Обновленные таблицы: `Products` (+spaceId), `Services` (+spaceId), `Users` (+defaultSpaceId)
- ✨ Новые API: SpaceRoutes, NewsRoutes, DocumentRoutes, ChatRoutes (WebSocket)
- 🔄 Обновленные API: ProductRoutes, ServiceRoutes (фильтрация по spaceId)

**Mobile:**
- ✨ Новые экраны: SpaceSelection, CreateSpace, JoinSpace, SpaceInfo, NewsList, DocumentsList, Chat
- 🔄 Обновленные экраны: ProductList, ServiceList (фильтр spaceId)
- ✨ Новые модули: SpaceManager, ThemeManager, ChatWebSocketClient
- 🔄 Навигация: RootComponent (Login → SpaceSelection → Main)

---

## 🎯 Целевая аудитория

- Жители посёлка (все возрастные группы)
- Локальное сообщество, которое уже общается в едином чате
- Пользователи, заинтересованные в местной торговле и услугах

## 🛠 Технологический стек

### Backend
- **Framework**: Ktor (Kotlin)
- **База данных**: PostgreSQL
- **ORM**: Exposed
- **Аутентификация**: JWT
- **Валидация**: Kotlin validation
- **API**: RESTful API
- **Хранилище файлов**: Локальное файловое хранилище / S3 (для изображений)

### Mobile (Client)
- **Framework**: Kotlin Multiplatform (KMP)
- **UI**: Compose Multiplatform
- **Навигация**: Voyager / Decompose
- **Networking**: Ktor Client
- **Сериализация**: Kotlinx Serialization
- **Локальное хранилище**: SQLDelight / Room
- **Изображения**: Coil / Kamel
- **DI**: Koin

### Платформы
- Android (основная)
- iOS (в перспективе)
- Desktop (опционально)

## 🎨 Основные функциональные возможности

### MVP (Минимально жизнеспособный продукт)

#### 1. Аутентификация и профили
- [ ] Регистрация пользователя (телефон/email)
- [ ] Вход в систему
- [ ] Профиль пользователя (имя, фото, контакты)
- [ ] Редактирование профиля

#### 2. Marketplace - Товары
- [ ] Создание объявления о продаже товара
  - Название, описание, цена
  - Фотографии (до 5 штук)
  - Категория товара
  - Состояние (новое/б/у)
- [ ] Просмотр списка товаров
- [ ] Фильтрация по категориям
- [ ] Поиск товаров
- [ ] Детальная карточка товара
- [ ] Контакт с продавцом (телефон, сообщение)
- [ ] Избранное
- [ ] Мои объявления (редактирование, удаление)

#### 3. Услуги
- [ ] Создание объявления об услуге
  - Название, описание
  - Категория услуги (ремонт, уборка, обучение и т.д.)
  - Стоимость / "Договорная"
  - Фото мастера/примеры работ
- [ ] Просмотр списка услуг
- [ ] Поиск услуг
- [ ] Контакт с исполнителем

#### 4. Основной функционал
- [ ] Категории товаров и услуг
- [ ] Пуш-уведомления о новых объявлениях
- [ ] Модерация объявлений (базовая)

### Фаза 2 - Расширенный функционал

#### 5. Социальные функции
- [ ] Лента новостей посёлка
  - Публикация новостей администраторами
  - Комментарии к новостям
  - Лайки
- [ ] Чат между покупателем и продавцом
- [ ] Рейтинг пользователей
- [ ] Отзывы о продавцах/исполнителях

#### 6. Расширенный marketplace
- [ ] Статусы объявлений (активно, продано, снято с публикации)
- [ ] История покупок/продаж
- [ ] Статистика просмотров объявления
- [ ] Возможность поднять объявление в топ
- [ ] Жалобы на объявления

#### 7. Дополнительные возможности
- [ ] Мероприятия и события в посёлке
- [ ] Опросы и голосования
- [ ] Доска объявлений администрации
- [ ] Карта посёлка с метками (магазины, услуги)
- [ ] Push-уведомления о важных событиях

### Фаза 3 - Продвинутые функции

#### 8. Монетизация и развитие
- [ ] Платное продвижение объявлений
- [ ] Премиум-аккаунты
- [ ] Интеграция с платёжными системами
- [ ] Аналитика для продавцов
- [ ] Бронирование услуг с календарём

## 📊 Модели данных

### User (Пользователь)
```kotlin
data class User(
    val id: Long,
    val phone: String,
    val email: String?,
    val name: String,
    val avatar: String?,
    val bio: String?,
    val rating: Double?,
    val createdAt: LocalDateTime,
    val isVerified: Boolean,
    val role: UserRole // USER, MODERATOR, ADMIN
)
```

### Product (Товар)
```kotlin
data class Product(
    val id: Long,
    val userId: Long,
    val title: String,
    val description: String,
    val price: Double,
    val categoryId: Long,
    val condition: ProductCondition, // NEW, USED
    val images: List<String>,
    val status: ProductStatus, // ACTIVE, SOLD, ARCHIVED
    val views: Int,
    val createdAt: LocalDateTime,
    val updatedAt: LocalDateTime
)
```

### Service (Услуга)
```kotlin
data class Service(
    val id: Long,
    val userId: Long,
    val title: String,
    val description: String,
    val categoryId: Long,
    val price: String?, // "1000" или "Договорная"
    val images: List<String>,
    val status: ServiceStatus, // ACTIVE, INACTIVE
    val views: Int,
    val createdAt: LocalDateTime,
    val updatedAt: LocalDateTime
)
```

### Category (Категория)
```kotlin
data class Category(
    val id: Long,
    val name: String,
    val icon: String?,
    val type: CategoryType // PRODUCT, SERVICE
)
```

### NewsPost (Новость) - Фаза 2
```kotlin
data class NewsPost(
    val id: Long,
    val authorId: Long,
    val title: String,
    val content: String,
    val images: List<String>,
    val likes: Int,
    val commentsCount: Int,
    val createdAt: LocalDateTime
)
```

## 🏗 Архитектура

### Backend (Ktor)

```
backend/
├── src/main/kotlin/
│   ├── com/socialspace/
│   │   ├── Application.kt
│   │   ├── plugins/
│   │   │   ├── Routing.kt
│   │   │   ├── Security.kt
│   │   │   ├── Serialization.kt
│   │   │   └── Database.kt
│   │   ├── routes/
│   │   │   ├── AuthRoutes.kt
│   │   │   ├── UserRoutes.kt
│   │   │   ├── ProductRoutes.kt
│   │   │   ├── ServiceRoutes.kt
│   │   │   └── CategoryRoutes.kt
│   │   ├── models/
│   │   │   ├── User.kt
│   │   │   ├── Product.kt
│   │   │   ├── Service.kt
│   │   │   └── Category.kt
│   │   ├── data/
│   │   │   ├── DatabaseFactory.kt
│   │   │   └── tables/
│   │   │       ├── Users.kt
│   │   │       ├── Products.kt
│   │   │       ├── Services.kt
│   │   │       └── Categories.kt
│   │   ├── repository/
│   │   │   ├── UserRepository.kt
│   │   │   ├── ProductRepository.kt
│   │   │   ├── ServiceRepository.kt
│   │   │   └── CategoryRepository.kt
│   │   ├── services/
│   │   │   ├── AuthService.kt
│   │   │   ├── FileService.kt
│   │   │   └── ValidationService.kt
│   │   └── utils/
│   │       ├── JwtConfig.kt
│   │       └── Extensions.kt
```

### Mobile (Kotlin Multiplatform + Compose)

```
shared/
├── commonMain/
│   ├── domain/
│   │   ├── models/
│   │   ├── repository/
│   │   └── usecases/
│   ├── data/
│   │   ├── api/
│   │   ├── repository/
│   │   └── local/
│   └── presentation/
│       ├── screens/
│       │   ├── auth/
│       │   ├── home/
│       │   ├── products/
│       │   ├── services/
│       │   └── profile/
│       ├── components/
│       └── navigation/

androidApp/
├── MainActivity.kt
└── AndroidManifest.xml

iosApp/
└── iosApp.swift
```

## 🚀 API Endpoints

### Authentication
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `POST /api/auth/refresh` - Обновление токена
- `GET /api/auth/me` - Текущий пользователь

### Users
- `GET /api/users/{id}` - Профиль пользователя
- `PUT /api/users/{id}` - Обновление профиля
- `POST /api/users/{id}/avatar` - Загрузка аватара

### Products
- `GET /api/products` - Список товаров (с пагинацией, фильтрами)
- `GET /api/products/{id}` - Детали товара
- `POST /api/products` - Создание товара
- `PUT /api/products/{id}` - Обновление товара
- `DELETE /api/products/{id}` - Удаление товара
- `GET /api/products/my` - Мои товары
- `POST /api/products/{id}/favorite` - Добавить в избранное
- `GET /api/products/favorites` - Избранные товары

### Services
- `GET /api/services` - Список услуг
- `GET /api/services/{id}` - Детали услуги
- `POST /api/services` - Создание услуги
- `PUT /api/services/{id}` - Обновление услуги
- `DELETE /api/services/{id}` - Удаление услуги
- `GET /api/services/my` - Мои услуги

### Categories
- `GET /api/categories` - Все категории
- `GET /api/categories/products` - Категории товаров
- `GET /api/categories/services` - Категории услуг

### Files
- `POST /api/upload` - Загрузка изображения

## 📅 План разработки по этапам

### Этап 1: Подготовка и настройка (1-2 недели)
1. ✅ Создание структуры проекта KMP
2. ✅ Настройка Backend проекта (Ktor)
3. ✅ Настройка базы данных (PostgreSQL + Exposed + H2 для разработки)
4. ✅ Настройка JWT аутентификации
5. [ ] Базовая структура Compose Multiplatform приложения
6. [ ] Настройка DI (Koin)
7. [ ] Настройка навигации

### Этап 2: Аутентификация и профили (1 неделя)
1. ✅ Backend: API аутентификации (регистрация, вход, получение текущего пользователя)
2. ✅ Backend: API профиля пользователя (получение, обновление, удаление)
3. ✅ Backend: Валидация входных данных
4. ✅ Backend: Repository слой для работы с БД
5. [ ] Mobile: Экраны входа/регистрации
6. [ ] Mobile: Хранение JWT токена
7. [ ] Mobile: Экран профиля

### Этап 3: Категории (3 дня)
1. ✅ Backend: Таблица категорий
2. ✅ Backend: Предзаполнение категорий
3. ✅ Backend: API категорий
4. [ ] Mobile: Отображение категорий

### Этап 4: Marketplace - Товары (2 недели)
1. ✅ Backend: CRUD API для товаров
2. [ ] Backend: Загрузка изображений
3. ✅ Backend: Пагинация и фильтрация
4. [ ] Mobile: Список товаров
5. [ ] Mobile: Создание объявления о товаре
6. [ ] Mobile: Детальная карточка товара
7. [ ] Mobile: Редактирование/удаление товара
8. [ ] Mobile: Поиск и фильтры
9. [ ] Mobile: Избранное

### Этап 5: Услуги (1 неделя)
1. [ ] Backend: CRUD API для услуг
2. [ ] Mobile: Список услуг
3. [ ] Mobile: Создание объявления об услуге
4. [ ] Mobile: Детальная карточка услуги
5. [ ] Mobile: Редактирование/удаление услуги

### Этап 6: Уведомления и полировка (1 неделя)
1. [ ] Backend: Push-уведомления (Firebase)
2. [ ] Mobile: Обработка уведомлений
3. [ ] Mobile: Улучшение UI/UX
4. [ ] Тестирование всех функций
5. [ ] Исправление багов

### Этап 7: Деплой MVP (3-5 дней)
1. [ ] Деплой Backend на сервер
2. [ ] Настройка домена и SSL
3. [ ] Публикация Android приложения (Google Play / APK)
4. [ ] Документация для пользователей
5. [ ] Бета-тестирование с жителями посёлка

### Этап 8: Фаза 2 - Социальные функции (по запросу)
1. [ ] Лента новостей
2. [ ] Чат между пользователями
3. [ ] Рейтинги и отзывы
4. [ ] Дополнительная модерация

## 🎯 Критерии успеха MVP

1. ✅ Пользователи могут зарегистрироваться и войти
2. ✅ Пользователи могут создавать объявления о товарах с фото
3. ✅ Пользователи могут просматривать товары других
4. ✅ Пользователи могут связаться с продавцом
5. ✅ Пользователи могут создавать объявления об услугах
6. ✅ Работает поиск и фильтрация
7. ✅ Стабильная работа на Android
8. ✅ Минимум 50 активных пользователей из посёлка

## 📝 Дополнительные заметки

### Безопасность
- Валидация всех входных данных
- Защита от SQL-инъекций (через Exposed)
- Rate limiting для API
- Модерация контента (ручная на начальном этапе)
- Защита от спама

### Производительность
- Кэширование на клиенте
- Ленивая загрузка изображений
- Пагинация списков
- Индексы в базе данных

### Масштабируемость
- Возможность добавления новых категорий
- Расширение функционала без breaking changes
- Модульная архитектура

## 🔄 Текущий статус

**Фаза**: Этап 6 - Загрузка изображений (ЗАВЕРШЕН! ✅)

**Последнее обновление**: 2025-11-09

### Что сделано:

#### ✅ Этап 1: Подготовка и настройка (ЗАВЕРШЕН)
- ✅ Создана структура Kotlin Multiplatform проекта
- ✅ Создан модуль `sharedUI` для общего кода (Compose Multiplatform)
- ✅ Создан модуль `backend` (Ktor Server)
- ✅ Настроены все зависимости (Ktor, Exposed, PostgreSQL, H2, JWT, BCrypt)
- ✅ Реализованы Ktor плагины (Serialization, Security, CORS, Logging, Routing, Database)
- ✅ Созданы модели данных (User, Product, Service, Category)
- ✅ Созданы таблицы БД через Exposed ORM (Users, Products, Services, Categories, Favorites)
- ✅ Настроена JWT аутентификация
- ✅ Реализованы утилиты для JWT токенов и хеширования паролей
- ✅ **H2 Database для локальной разработки** (решена проблема с PostgreSQL)
- ✅ **Docker Compose для PostgreSQL** (для production)

#### ✅ Этап 2: Backend для аутентификации и профилей (ЗАВЕРШЕН)
- ✅ **UserRepository** - полноценный репозиторий для работы с пользователями в БД
- ✅ **DTO модели** - Request/Response модели для API (ErrorResponse, SuccessResponse, UpdateProfileRequest и др.)
- ✅ **ValidationService** - валидация телефона, email, пароля, имени, биографии с нормализацией данных
- ✅ **AuthService** - бизнес-логика регистрации, входа, обновления профиля
- ✅ **AuthRoutes** - endpoints для аутентификации:
  - `POST /api/auth/register` - Регистрация
  - `POST /api/auth/login` - Вход
  - `GET /api/auth/me` - Текущий пользователь
- ✅ **UserRoutes** - endpoints для профилей:
  - `GET /api/users/{id}` - Публичный профиль
  - `PUT /api/users/{id}` - Обновление профиля
  - `DELETE /api/users/{id}` - Удаление аккаунта
- ✅ **API документация** - полная документация всех endpoints с примерами (backend/API_DOCUMENTATION.md)

#### ✅ Этап 3: Backend для категорий (ЗАВЕРШЕН)
- ✅ **CategoryRepository** - репозиторий для работы с категориями в БД:
  - getAllCategories() - получить все категории
  - getCategoriesByType() - получить категории по типу (PRODUCT/SERVICE)
  - findById() - найти категорию по ID
  - populateDefaultCategories() - предзаполнение категорий
- ✅ **CategoryRoutes** - endpoints для категорий:
  - `GET /api/categories` - Все категории
  - `GET /api/categories/products` - Категории товаров
  - `GET /api/categories/services` - Категории услуг
  - `GET /api/categories/{id}` - Категория по ID
- ✅ **Seed Data** - предзаполнение категорий при старте:
  - 15 категорий товаров (Мебель, Электроника, Детские товары и т.д.)
  - 15 категорий услуг (Ремонт, Уборка, Репетиторство и т.д.)
  - Все категории с эмодзи иконками для UI
- ✅ **API документация обновлена** - добавлены примеры для Category endpoints

#### ✅ Этап 4: Backend для товаров (ЗАВЕРШЕН)
- ✅ **ProductRepository** - полноценный репозиторий для работы с товарами в БД:
  - createProduct() - создание товара
  - findById() - поиск товара по ID
  - findByUserId() - найти все товары пользователя
  - getAllProducts() - список всех товаров с фильтрацией и пагинацией
  - countProducts() - подсчет товаров с учетом фильтров
  - updateProduct() - обновление товара
  - deleteProduct() - удаление товара
  - incrementViews() - увеличение счетчика просмотров
  - addToFavorites() - добавить в избранное
  - removeFromFavorites() - удалить из избранного
  - isFavorite() - проверка наличия в избранном
  - getFavorites() - получить избранные товары
  - countFavorites() - подсчет избранных товаров
  - isOwner() - проверка владельца товара
- ✅ **DTO модели** - Request/Response модели для Product API:
  - CreateProductRequest - создание товара
  - UpdateProductRequest - обновление товара (все поля опциональные)
  - ProductResponse - расширенный ответ с информацией о пользователе и категории
  - ProductListResponse - ответ со списком товаров и пагинацией
  - UserPublicInfo - публичная информация о пользователе
  - CategoryInfo - краткая информация о категории
- ✅ **ProductRoutes** - полноценный CRUD API для товаров:
  - `GET /api/products` - Список товаров с пагинацией и фильтрами (categoryId, status, condition, minPrice, maxPrice, search, page, pageSize)
  - `GET /api/products/{id}` - Детали товара с информацией о пользователе и категории
  - `POST /api/products` - Создание товара (требует JWT)
  - `PUT /api/products/{id}` - Обновление товара (требует JWT, только свой товар)
  - `DELETE /api/products/{id}` - Удаление товара (требует JWT, только свой товар)
  - `GET /api/products/my` - Мои товары (требует JWT)
  - `POST /api/products/{id}/favorite` - Добавить в избранное (требует JWT)
  - `DELETE /api/products/{id}/favorite` - Удалить из избранного (требует JWT)
  - `GET /api/products/favorites` - Избранные товары с пагинацией (требует JWT)
- ✅ **Валидация данных**:
  - Название: 1-200 символов, обязательное
  - Описание: минимум 1 символ, обязательное
  - Цена: не может быть отрицательной, обязательное
  - Изображения: 1-5 изображений, обязательное
  - Категория: проверка существования и типа (PRODUCT)
  - Права доступа: только владелец может редактировать/удалять
- ✅ **Функции фильтрации и поиска**:
  - Фильтрация по категории, статусу, состоянию, диапазону цен
  - Полнотекстовый поиск по названию и описанию (case-insensitive)
  - Пагинация с настраиваемым размером страницы (1-100)
  - Подсчет общего количества товаров с учетом фильтров
- ✅ **Избранное**:
  - Уникальный индекс (userId, productId) для предотвращения дубликатов
  - Проверка существования товара перед добавлением
  - Каскадное удаление при удалении товара
- ✅ **API документация обновлена** - добавлены 9 новых endpoints для Products API с примерами

#### ✅ Этап 5: Backend для услуг (ЗАВЕРШЕН)
- ✅ **ServiceRepository** - полноценный репозиторий для работы с услугами в БД:
  - createService() - создание услуги
  - findById() - поиск услуги по ID
  - findByUserId() - найти все услуги пользователя
  - getAllServices() - список всех услуг с фильтрацией и пагинацией
  - countServices() - подсчет услуг с учетом фильтров
  - updateService() - обновление услуги
  - deleteService() - удаление услуги
  - incrementViews() - увеличение счетчика просмотров
  - isOwner() - проверка владельца услуги
- ✅ **DTO модели** - Request/Response модели для Service API:
  - CreateServiceRequest - создание услуги
  - UpdateServiceRequest - обновление услуги (все поля опциональные, включая images)
  - ServiceResponse - расширенный ответ с информацией о пользователе и категории
  - ServiceListResponse - ответ со списком услуг и пагинацией
- ✅ **ServiceRoutes** - полноценный CRUD API для услуг:
  - `GET /api/services` - Список услуг с пагинацией и фильтрами (categoryId, status, search, page, pageSize)
  - `GET /api/services/{id}` - Детали услуги с информацией о пользователе и категории
  - `POST /api/services` - Создание услуги (требует JWT)
  - `PUT /api/services/{id}` - Обновление услуги (требует JWT, только свою услугу)
  - `DELETE /api/services/{id}` - Удаление услуги (требует JWT, только свою услугу)
  - `GET /api/services/my` - Мои услуги (требует JWT)
- ✅ **Валидация данных**:
  - Название: 1-200 символов, обязательное
  - Описание: минимум 1 символ, обязательное
  - Цена: опциональное поле, может быть null или строка (например "1000" или "Договорная")
  - Изображения: 1-5 изображений, обязательное
  - Категория: проверка существования и типа (SERVICE)
  - Права доступа: только владелец может редактировать/удалять
- ✅ **Функции фильтрации и поиска**:
  - Фильтрация по категории, статусу
  - Полнотекстовый поиск по названию и описанию (case-insensitive)
  - Пагинация с настраиваемым размером страницы (1-100)
  - Подсчет общего количества услуг с учетом фильтров
- ✅ **API документация обновлена** - добавлены 6 новых endpoints для Services API с примерами и curl запросами

#### ✅ Этап 6: Backend для загрузки изображений (ЗАВЕРШЕН)
- ✅ **FileService** - сервис для работы с файлами (загрузка, валидация, хранение):
  - validateImage() - валидация изображений (формат, размер, MIME type)
  - saveFile() - сохранение файла с генерацией UUID имени
  - deleteFile() - удаление файла
  - deleteFiles() - массовое удаление файлов
  - Поддержка форматов: JPG, JPEG, PNG, WEBP
  - Максимальный размер файла: 5 MB
  - Структура хранилища: uploads/avatars/, uploads/products/, uploads/services/
- ✅ **FileUploadResponse DTO** - модель ответа при загрузке файла
- ✅ **FileRoutes** - endpoints для загрузки файлов:
  - `POST /api/upload?type={type}` - Универсальная загрузка изображений (требует JWT)
  - `POST /api/users/{id}/avatar` - Загрузка аватара с автообновлением профиля (требует JWT)
- ✅ **StaticFiles plugin** - раздача загруженных файлов через `/uploads/*`
- ✅ **Multipart/form-data** - обработка загрузки файлов через multipart
- ✅ **Автоматическое создание директорий** - uploads/, avatars/, products/, services/
- ✅ **Удаление старых файлов** - при обновлении аватара удаляется предыдущий
- ✅ **API документация обновлена** - добавлены 2 новых endpoints для File Upload API с curl примерами

### Текущая структура проекта:
```
SocialSpace/
├── api-models/                 # ✅ Kotlin Multiplatform модуль - единый источник истины для API моделей
│   ├── build.gradle.kts        # Kotlin Multiplatform (JVM + Android + iOS)
│   └── src/commonMain/kotlin/info/javaway/sc/api/models/
│       ├── User.kt             # User, UserRole, UserPublicInfo, PublicUserProfile
│       ├── UserDto.kt          # RegisterRequest, LoginRequest, AuthResponse, etc.
│       ├── Product.kt          # Product, ProductCondition, ProductStatus
│       ├── ProductDto.kt       # DTO для Product API
│       ├── Category.kt         # Category, CategoryType, CategoryInfo
│       ├── Service.kt          # Service, ServiceStatus
│       ├── ServiceDto.kt       # DTO для Service API
│       └── ApiResponse.kt      # ErrorResponse, SuccessResponse, FileUploadResponse
├── sharedUI/                   # Kotlin Multiplatform модуль (UI + бизнес-логика)
│   └── build.gradle.kts        # ✅ Зависит от api-models
├── androidApp/                 # Android приложение
├── backend/                    # Ktor сервер (REST API)
│   ├── build.gradle.kts        # ✅ Зависит от api-models
│   ├── src/main/kotlin/
│   │   ├── Application.kt
│   │   ├── plugins/            # Ktor плагины
│   │   ├── routes/             # ✅ AuthRoutes, UserRoutes, CategoryRoutes, ProductRoutes, ServiceRoutes, FileRoutes
│   │   ├── data/tables/        # ✅ Exposed таблицы
│   │   ├── repository/         # ✅ UserRepository, CategoryRepository, ProductRepository, ServiceRepository
│   │   ├── services/           # ✅ AuthService, ValidationService, FileService
│   │   └── utils/              # ✅ JwtConfig, PasswordHasher
│   ├── API_DOCUMENTATION.md    # ✅ Документация API
│   ├── uploads/                # ✅ Директория для загруженных файлов (avatars/, products/, services/)
│   └── resources/
│       └── application.conf    # ✅ Конфигурация (H2 по умолчанию)
├── docker-compose.yml          # ✅ PostgreSQL для production
├── .env.example                # ✅ Пример переменных окружения
└── CLAUDE.md                   # Документация проекта
```

### Реализованные API Endpoints:

#### 🔐 Аутентификация
- ✅ `POST /api/auth/register` - Регистрация с валидацией
- ✅ `POST /api/auth/login` - Вход с проверкой пароля
- ✅ `GET /api/auth/me` - Получение текущего пользователя (требует JWT)

#### 👤 Пользователи
- ✅ `GET /api/users/{id}` - Публичный профиль
- ✅ `PUT /api/users/{id}` - Обновление профиля (требует JWT, только свой профиль)
- ✅ `DELETE /api/users/{id}` - Удаление аккаунта (требует JWT, только свой аккаунт)

#### 📂 Категории
- ✅ `GET /api/categories` - Все категории
- ✅ `GET /api/categories/products` - Категории товаров
- ✅ `GET /api/categories/services` - Категории услуг
- ✅ `GET /api/categories/{id}` - Категория по ID

#### 🛍 Товары
- ✅ `GET /api/products` - Список товаров с пагинацией и фильтрами
- ✅ `GET /api/products/{id}` - Детали товара (увеличивает счетчик просмотров)
- ✅ `POST /api/products` - Создание товара (требует JWT)
- ✅ `PUT /api/products/{id}` - Обновление товара (требует JWT, только свой)
- ✅ `DELETE /api/products/{id}` - Удаление товара (требует JWT, только свой)
- ✅ `GET /api/products/my` - Мои товары (требует JWT)
- ✅ `POST /api/products/{id}/favorite` - Добавить в избранное (требует JWT)
- ✅ `DELETE /api/products/{id}/favorite` - Удалить из избранного (требует JWT)
- ✅ `GET /api/products/favorites` - Избранные товары с пагинацией (требует JWT)

#### 🔧 Услуги
- ✅ `GET /api/services` - Список услуг с пагинацией и фильтрами
- ✅ `GET /api/services/{id}` - Детали услуги (увеличивает счетчик просмотров)
- ✅ `POST /api/services` - Создание услуги (требует JWT)
- ✅ `PUT /api/services/{id}` - Обновление услуги (требует JWT, только свою)
- ✅ `DELETE /api/services/{id}` - Удаление услуги (требует JWT, только свою)
- ✅ `GET /api/services/my` - Мои услуги (требует JWT)

#### 📁 Загрузка файлов
- ✅ `POST /api/upload?type={type}` - Универсальная загрузка изображений (требует JWT)
- ✅ `POST /api/users/{id}/avatar` - Загрузка аватара пользователя (требует JWT, только свой)
- ✅ `GET /uploads/{type}/{filename}` - Доступ к загруженным файлам (публичный)

### Особенности реализации:
- ✅ **JWT аутентификация** с защищенными endpoints
- ✅ **Валидация данных**: телефон (с нормализацией к +7XXXXXXXXXX), email, пароль (мин. 6 символов), товары (название, цена, изображения), услуги (название, описание, изображения), файлы (формат, размер, MIME type)
- ✅ **Безопасность**: BCrypt для хеширования паролей, проверка прав доступа, владение ресурсами
- ✅ **Обработка ошибок**: структурированные ErrorResponse с понятными сообщениями
- ✅ **H2 для разработки**: не требует установки PostgreSQL
- ✅ **PostgreSQL для production**: через Docker Compose
- ✅ **Пагинация**: гибкая пагинация с настраиваемым размером страницы (1-100)
- ✅ **Фильтрация и поиск**: мощная система фильтров (категория, статус, состояние, цена) и полнотекстовый поиск
- ✅ **Избранное**: система избранных товаров с уникальными индексами и каскадным удалением
- ✅ **Загрузка файлов**: Multipart/form-data, валидация изображений (JPG, PNG, WEBP, макс. 5 MB), UUID генерация имен, автоматическое создание директорий, раздача статических файлов

### 📊 Архитектурный обзор Backend (2025-11-09)

**Статус:** ✅ Завершен

#### Что проверено:
- 32 Kotlin файла
- Все ключевые компоненты (Repository, Service, Routes, Plugins)
- Безопасность, производительность, архитектурные паттерны

#### ✅ Сильные стороны:
- **Архитектура:** Clean Architecture с четким разделением слоев (Repository → Service → Routes)
- **Безопасность:** BCrypt для паролей, JWT аутентификация, Exposed ORM (защита от SQL injection)
- **Валидация:** Полная валидация входных данных (телефон, email, пароль, файлы)
- **Health Check:** Endpoint `/health` уже реализован (Routing.kt:19-21)
- **Обработка ошибок:** Структурированные ErrorResponse, try-catch блоки

#### ⚠️ Технический долг (для Production):
1. **JWT Secret hardcoded** - переместить в environment variables (JwtConfig.kt:8)
2. **CORS anyHost()** - настроить specific origins для production (HTTP.kt:18)
3. **Нет Rate Limiting** - добавить защиту от DDoS/spam/brute-force
4. **Нет Request Timeout** - может привести к зависанию
5. **Базовое логирование** - нет correlation IDs для трассировки
6. **Health Check упрощенный** - не проверяет подключение к БД

**Решение:** Технический долг будет устранен после MVP Mobile приложения. Для локальной разработки текущая реализация достаточна.

---

### Следующие шаги:

**✅ Backend MVP ЗАВЕРШЕН!** Все основные функции реализованы:
- ✅ Аутентификация и профили пользователей
- ✅ Категории товаров и услуг
- ✅ CRUD для товаров с избранным
- ✅ CRUD для услуг
- ✅ Загрузка и хранение изображений
- ✅ Архитектурный обзор проведен

**🚀 ТЕКУЩАЯ ФАЗА: Mobile разработка (Kotlin Multiplatform + Compose)**

#### ✅ Этап 7: Базовая настройка Mobile приложения (ЗАВЕРШЕН)
1. ✅ Проверить текущую структуру sharedUI и androidApp модулей
2. ✅ Настроить Ktor Client для работы с Backend API
3. ✅ Настроить Koin для Dependency Injection
4. ⏳ Настроить навигацию (Voyager или Decompose) - ОТЛОЖЕНО до следующего этапа
5. ✅ Создать базовые модели данных (DTO для API)
6. ✅ Реализовать ApiClient для взаимодействия с Backend

**Что реализовано:**
- 📁 Структура папок: `domain/`, `data/`, `presentation/`
- 📦 Модели данных: User, Category, Product, Service, Auth, ApiResponse
- 🌐 ApiClient с Ktor Client (все endpoints Backend API)
- 💾 TokenManager для хранения JWT токена (Multiplatform Settings)
- 🗂 Repository слой (AuthRepository, CategoryRepository, ProductRepository, ServiceRepository)
- 💉 Koin DI модули (dataModule, repositoryModule, viewModelModule)
- 🎨 Material Design 3 тема (цвета, типография)

#### ✅ Этап 8: Аутентификация (Mobile UI) - ЗАВЕРШЕН! 🎉
1. ✅ Экран входа (LoginScreen) - полностью готов
2. ✅ Экран регистрации (RegisterScreen) - готов с полной валидацией
3. ✅ Хранение JWT токена (TokenManager с Multiplatform Settings)
4. ✅ AuthRepository для работы с API
5. ✅ LoginViewModel с состояниями (Loading, Success, Error)
6. ✅ RegisterViewModel с валидацией (телефон, имя, email, пароль, подтверждение)
7. ✅ HomeScreen - базовый экран после авторизации
8. ✅ HomeViewModel - загрузка профиля пользователя
9. ✅ Decompose Navigation (RootComponent) - полная навигация Login → Register → Home
10. ✅ RootContent - отображение экранов с анимацией fade
11. ✅ Обновлен AppModule (Koin DI) - добавлены RegisterViewModel и HomeViewModel
12. ✅ Обновлен App.kt и MainActivity.kt для работы с RootComponent

**Что реализовано в Этапе 8:**
- 📱 **RegisterScreen**: форма регистрации с полями (телефон, имя, email, пароль, подтверждение)
- ✅ **Валидация**: проверка формата телефона (+7XXXXXXXXXX), email, длины пароля, совпадения паролей
- 🏠 **HomeScreen**: приветствие, карточка профиля, кнопка "Выйти"
- 🧭 **Decompose Navigation**: ChildStack для управления экранами с анимацией
- 🔐 **Автоматический вход**: если есть JWT токен, сразу переход на HomeScreen
- 🚪 **Выход из системы**: очистка токена и возврат на LoginScreen

#### Этап 9: Главный экран и список товаров
1. [ ] Главный экран с Bottom Navigation
2. [ ] Список товаров (ProductList Screen) с LazyColumn
3. [ ] Карточка товара (ProductCard Composable)
4. [ ] Загрузка изображений (Coil или Kamel)
5. [ ] Pull-to-refresh и пагинация
6. [ ] ProductRepository и ProductViewModel

#### Этап 10: Детальная карточка и профиль
1. [ ] Экран деталей товара (ProductDetails Screen)
2. [ ] Кнопка "Связаться с продавцом" (телефон)
3. [ ] Добавление в избранное
4. [ ] Экран профиля пользователя
5. [ ] Редактирование профиля
6. [ ] Мои объявления

#### Этап 11: Создание объявлений
1. [ ] Экран создания товара
2. [ ] Загрузка фотографий (Camera/Gallery)
3. [ ] Выбор категории
4. [ ] Валидация формы
5. [ ] Экран создания услуги
6. [ ] ProductCreateViewModel и ServiceCreateViewModel

#### Этап 12: Категории и услуги
1. [ ] Экран категорий
2. [ ] Фильтрация по категориям
3. [ ] Список услуг (ServiceList Screen)
4. [ ] Детальная карточка услуги
5. [ ] Поиск товаров и услуг

**✅ Этап 7 ЗАВЕРШЕН!** Базовая архитектура Mobile приложения готова.

**✅ Этап 8 ЗАВЕРШЕН!** Аутентификация с полной навигацией готова! 🎉

**✅ Этап 9 ЗАВЕРШЕН!** Главный экран с Bottom Navigation и список товаров готовы! 🎉

#### ✅ Этап 9: Главный экран и список товаров (ЗАВЕРШЕН)
1. ✅ **MainComponent** - компонент с Bottom Navigation (3 вкладки: Товары, Услуги, Профиль)
2. ✅ **MainContent** - UI для Bottom Navigation с навигацией между вкладками
3. ✅ **ProductListViewModel** - управление состоянием списка товаров:
   - Состояния: Loading, Success, Error, Empty
   - Pull-to-refresh
   - Пагинация (загрузка следующих страниц)
   - Загрузка товаров через ProductRepository
4. ✅ **ProductListScreen** - экран списка товаров:
   - LazyColumn для отображения списка
   - Pull-to-refresh (ExperimentalMaterialApi)
   - Автоматическая пагинация при прокрутке
   - Обработка всех состояний (Loading, Success, Error, Empty)
5. ✅ **ProductCard** - Composable карточка товара:
   - Изображение товара (Coil AsyncImage)
   - Название, цена, категория, состояние (NEW/USED)
   - Количество просмотров
   - Красивый Material Design 3 дизайн
6. ✅ **Интеграция Coil** - загрузка изображений:
   - SubcomposeAsyncImage для изображений товаров
   - Loading indicator во время загрузки
   - Error placeholder при ошибке
   - Crossfade анимация
7. ✅ **Интеграция в RootComponent** - MainComponent заменяет Home экран после авторизации
8. ✅ **Обновлен Koin DI** - добавлен ProductListViewModel

**Что реализовано в Этапе 9:**
- 🎨 **Bottom Navigation**: 3 вкладки (Товары, Услуги, Профиль) с Material Icons
- 📦 **Список товаров**: полноценный список с карточками товаров из Backend
- 🖼 **Загрузка изображений**: Coil загружает изображения с Backend (http://10.0.2.2:8080/uploads/*)
- 🔄 **Pull-to-refresh**: обновление списка свайпом вниз
- 📄 **Пагинация**: автоматическая подгрузка при достижении конца списка
- ✨ **Material Design 3**: современный дизайн с карточками, цветами, типографикой

**🚧 Текущий этап:** Этап 9.2 - Рефакторинг пагинации с Paging 3 (ЗАВЕРШЕН! ✅)

### 🐛 Решена проблема: Бесконечный лоадер на экране товаров

**Дата**: 2025-11-10

**Проблема:**
- На экране товаров бесконечно крутился лоадер
- Моковые данные с сервера не отображались

**Причины:**
1. ❌ **Неправильная обработка Result в ProductListViewModel** - использовался `as?` вместо `when`
2. ❌ **Backend не запущен** - нет процесса на порту 8080
3. ❌ **Нет тестовых данных в БД** - пустая база после первого запуска

**Решение:**

1. ✅ **Исправлена обработка Result** в `ProductListViewModel.kt`:
   - Заменено `as? Result.Success` на `when (val result = ...)` во всех методах
   - Добавлено логирование ошибок через Napier
   - Теперь все ошибки API корректно обрабатываются и отображаются

2. ✅ **Создан скрипт для добавления тестовых данных**:
   - Файл: `backend/add_test_data.sh`
   - Автоматически создаёт 2 пользователей и 6 товаров
   - Тестовые аккаунты: `+79991234567` и `+79997654321` (пароль: `password123`)

3. ✅ **Создана инструкция для пользователя**:
   - Файл: `QUICK_START.md`
   - Как запустить backend (IntelliJ IDEA / Gradle / CLI)
   - Как добавить тестовые данные
   - Troubleshooting для типичных проблем

**Файлы изменены:**
- ✅ `sharedUI/src/.../ProductListViewModel.kt` - исправлена обработка ошибок
- ✅ `backend/add_test_data.sh` - скрипт добавления тестовых данных (новый файл)
- ✅ `QUICK_START.md` - инструкция быстрого старта (новый файл)

**Как проверить работу:**
1. Запустить backend: `./gradlew :backend:run` или через IntelliJ IDEA
2. Добавить тестовые данные: `cd backend && ./add_test_data.sh`
3. Запустить Android приложение
4. Войти с тестовым аккаунтом: `+79991234567` / `password123`
5. Перейти на вкладку "Товары" - должно отобразиться 6 товаров

---

### 📄 Рефакторинг пагинации с Paging 3 (2025-11-11)

**Статус:** ✅ Завершен

**Проблема:**
Кастомная пагинация была реализована "на коленке" с ручным управлением страницами, состояниями и склеиванием списков. Это приводило к дублированию кода и потенциальным багам.

**Решение: Paging 3 library от Android Jetpack** ✅

#### Что реализовано:

1. ✅ **BasePagingSource<T>** - универсальный базовый класс для всех сущностей:
   - Инкапсулирует общую логику пагинации
   - Автоматическое определение nextKey/prevKey
   - Логирование через Napier
   - Обработка ошибок
   - Легко расширяется для Product, Service, Favorites и любых других сущностей

2. ✅ **ProductPagingSource** - реализация для товаров:
   - Наследует BasePagingSource<Product>
   - ProductFilters data class для фильтров
   - Интеграция с ApiClient
   - Преобразование DTO → Domain моделей

3. ✅ **ProductRepository.getProductsPaged()** - новый метод:
   - Возвращает `Flow<PagingData<Product>>`
   - PagingConfig с настройками (pageSize=20, prefetchDistance=5)
   - Старый метод getProducts() помечен @Deprecated

4. ✅ **ProductListViewModel** - полностью переписан:
   - `productsFlow: Flow<PagingData<Product>>` вместо ручного управления
   - Фильтры через StateFlow с автоматическим refresh
   - `flatMapLatest` для пересоздания PagingSource при изменении фильтров
   - `cachedIn(viewModelScope)` для кэширования данных

5. ✅ **ProductListScreen** - интеграция с Paging 3:
   - `collectAsLazyPagingItems()` для получения LazyPagingItems
   - Автоматическая обработка LoadState (Loading, Error, NotLoading)
   - Retry логика из коробки
   - Индикаторы загрузки для refresh и append
   - Обработка ошибок на уровне первой загрузки и пагинации

6. ✅ **Документация** - создан файл `sharedUI/PAGING3_GUIDE.md`:
   - Полное описание архитектуры
   - Примеры использования для Product
   - Пошаговая инструкция как добавить пагинацию для Service
   - Конфигурация PagingConfig
   - Best practices

#### Преимущества Paging 3:

- ✅ **Автоматическая пагинация** - библиотека управляет загрузкой страниц
- ✅ **Кэширование** - данные переживают configuration changes
- ✅ **Retry логика** - встроенная поддержка повтора запросов
- ✅ **LoadState** - удобные состояния Loading/Error/NotLoading
- ✅ **Pull-to-refresh** - `lazyPagingItems.refresh()` из коробки
- ✅ **Расширяемость** - легко добавлять новые сущности через BasePagingSource
- ✅ **Производительность** - эффективное использование памяти
- ✅ **Стандартизация** - официальная библиотека Android Jetpack

#### Структура кода:

```
sharedUI/src/commonMain/kotlin/
├── data/paging/
│   ├── BasePagingSource.kt         # ✅ Базовый класс для всех PagingSource
│   └── ProductPagingSource.kt      # ✅ PagingSource для товаров + ProductFilters
├── domain/repository/
│   └── ProductRepository.kt        # ✅ getProductsPaged(): Flow<PagingData<Product>>
├── data/repository/
│   └── ProductRepositoryImpl.kt    # ✅ Реализация с Pager + PagingConfig
├── presentation/screens/products/
│   ├── ProductListViewModel.kt     # ✅ Flow<PagingData> + фильтры + cachedIn()
│   └── ProductListScreen.kt        # ✅ collectAsLazyPagingItems() + LoadState
└── PAGING3_GUIDE.md                # ✅ Полная документация
```

#### Зависимости:

```toml
# gradle/libs.versions.toml
paging = "3.3.6"
paging-compose = "3.3.6"

paging-common = { module = "androidx.paging:paging-common", version.ref = "paging" }
paging-compose = { module = "androidx.paging:paging-compose", version.ref = "paging-compose" }
```

#### Как добавить пагинацию для Services:

1. Создать `ServicePagingSource` аналогично `ProductPagingSource`
2. Добавить `getServicesPaged()` в `ServiceRepository`
3. Обновить `ServiceListViewModel` для работы с `Flow<PagingData<Service>>`
4. Обновить `ServiceListScreen` с `collectAsLazyPagingItems()`

Подробная инструкция в файле `sharedUI/PAGING3_GUIDE.md`.

#### Файлы изменены:

- ✅ `gradle/libs.versions.toml` - добавлены версии Paging 3
- ✅ `sharedUI/build.gradle.kts` - зависимости paging-common и paging-compose
- ✅ `data/paging/BasePagingSource.kt` - новый файл
- ✅ `data/paging/ProductPagingSource.kt` - новый файл
- ✅ `domain/repository/ProductRepository.kt` - добавлен getProductsPaged()
- ✅ `data/repository/ProductRepositoryImpl.kt` - реализация с Pager
- ✅ `presentation/screens/products/ProductListViewModel.kt` - переписан с нуля
- ✅ `presentation/screens/products/ProductListScreen.kt` - переписан с нуля
- ✅ `sharedUI/PAGING3_GUIDE.md` - новый файл с документацией

**Результат:** Профессиональная архитектура пагинации, готовая к масштабированию для Services, Favorites, Search и других сущностей! 🚀

---

**🚧 Текущий этап:** Этап 10.1 - Детальная карточка товара (ЗАВЕРШЕН! ✅)

#### ✅ Этап 10.1: Детальная карточка товара (ProductDetailScreen) - ЗАВЕРШЕН! 🎉

**Дата**: 2025-11-11

**Что реализовано:**

1. ✅ **ProductDetailViewModel** - ViewModel с MVI подходом:
   - Состояния: Loading, Success(product, isFavorite, isOwner), Error
   - loadProduct() - загрузка товара с сервера
   - toggleFavorite() - добавление/удаление из избранного (оптимистичное обновление UI)
   - Автоматическая загрузка товара при инициализации
   - Обработка ошибок с логированием через Napier

2. ✅ **ProductDetailScreen** - UI экран деталей товара:
   - **Галерея изображений** с HorizontalPager (свайп между фото)
   - Индикаторы страниц (dots) для навигации по фото
   - **Информация о товаре**: название, цена, описание, категория, состояние
   - **Карточка продавца**: имя, телефон, кнопка "Позвонить"
   - **Дополнительная информация**: просмотры, дата создания
   - **TopAppBar** с кнопками "Назад" и "Избранное" (иконка меняется)
   - Обработка всех состояний: Loading, Success, Error
   - Кнопка "Повторить" при ошибке загрузки

3. ✅ **Навигация MainComponent**:
   - Добавлен Config.ProductDetail(productId: Long)
   - Добавлен Child.ProductDetail(productId, onBack)
   - Child.Products теперь передает callback onProductClick
   - Навигация через navigation.push() и navigation.pop()

4. ✅ **Интеграция в MainContent**:
   - ProductListScreen получает onProductClick callback
   - ProductDetailScreen отображается при клике на товар
   - Callback onCallSeller для звонка продавцу (заглушка)

5. ✅ **Koin DI**:
   - Добавлен ProductDetailViewModel с параметром productId
   - factory { (productId: Long) -> ... } для динамического создания

**Технические детали:**

- **HorizontalPager** из `androidx.compose.foundation.pager` для свайпа изображений
- **Coil** для загрузки изображений с плейсхолдерами
- **Material Design 3**: Cards, Chips, Colors, Typography
- **MVI архитектура**: единое состояние экрана (sealed interface)
- **Оптимистичное UI**: toggleFavorite сразу меняет UI, откат при ошибке
- **Decompose Navigation**: push/pop для навигации между экранами

**Файлы созданы/изменены:**
- ✅ `ProductDetailViewModel.kt` - новый файл (141 строка)
- ✅ `ProductDetailScreen.kt` - новый файл (429 строк)
- ✅ `MainComponent.kt` - добавлена навигация к деталям товара
- ✅ `MainContent.kt` - интеграция ProductDetailScreen
- ✅ `AppModule.kt` - добавлен ProductDetailViewModel в Koin DI

**TODO (следующие этапы):**
- [ ] **Android Intent для звонка**: реализовать Intent.ACTION_DIAL в androidApp
- [ ] **Проверка isFavorite**: получать статус избранного с Backend
- [ ] **Извлечение userId из JWT**: для проверки isOwner
- [ ] **Кнопка "Редактировать"**: для владельца товара

**User Journey:** ✅ Список товаров → Клик на товар → Детали → Кнопка "Позвонить" (готова)

---

**🚧 Текущий этап:** Этап 11 - Раздел Услуг (Services Section) - ЗАВЕРШЕН! ✅

#### ✅ Этап 11: Раздел Услуг (Services) - ЗАВЕРШЕН! 🎉

**Дата**: 2025-11-12

**Что реализовано:**

1. ✅ **ServicePagingSource** - пагинация для услуг с Paging 3:
   - ServiceFilters data class (categoryId, status, search)
   - Наследует BasePagingSource<Service>
   - Интеграция с ApiClient.getServices()
   - Преобразование DTO → Domain моделей

2. ✅ **ServiceRepository.getServicesPaged()** - новый метод:
   - Возвращает `Flow<PagingData<Service>>`
   - PagingConfig с настройками (pageSize=20, prefetchDistance=5)
   - Старый метод getServices() помечен @Deprecated

3. ✅ **ServiceListViewModel** - управление списком услуг:
   - `servicesFlow: Flow<PagingData<Service>>` с автоматической пагинацией
   - Фильтры через StateFlow (categoryId, status, search)
   - `flatMapLatest` для пересоздания PagingSource при изменении фильтров
   - `cachedIn(viewModelScope)` для кэширования данных

4. ✅ **ServiceCard** - Composable карточка услуги:
   - Изображение услуги (Coil AsyncImage)
   - Название, категория, цена (или "Договорная")
   - Количество просмотров
   - Material Design 3 дизайн (без condition, т.к. у услуг нет состояния NEW/USED)

5. ✅ **ServiceListScreen** - экран списка услуг:
   - `collectAsLazyPagingItems()` для получения LazyPagingItems
   - Автоматическая обработка LoadState (Loading, Error, NotLoading)
   - Retry логика из коробки
   - Индикаторы загрузки для refresh и append
   - Обработка ошибок на уровне первой загрузки и пагинации

6. ✅ **Интеграция в MainComponent**:
   - Child.Services изменен на data class с onServiceClick callback
   - Навигация готова для детальной страницы услуги (TODO: ServiceDetail)

7. ✅ **Интеграция в MainContent**:
   - ServiceListScreen заменил PlaceholderScreen("Услуги")
   - onServiceClick callback передается в ServiceListScreen

8. ✅ **Koin DI**:
   - Добавлен ServiceListViewModel в viewModelModule
   - factoryOf(::ServiceListViewModel)

**Технические детали:**

- **Paging 3**: автоматическая пагинация с кэшированием
- **Coil**: загрузка изображений услуг с Backend (http://10.0.2.2:8080/uploads/*)
- **Material Design 3**: единый стиль с ProductCard
- **BasePagingSource**: переиспользование базовой логики пагинации
- **ServiceFilters**: упрощенные фильтры (без condition, minPrice, maxPrice)

**Файлы созданы/изменены:**
- ✅ `data/paging/ServicePagingSource.kt` - новый файл (62 строки)
- ✅ `domain/repository/ServiceRepository.kt` - добавлен getServicesPaged()
- ✅ `data/repository/ServiceRepositoryImpl.kt` - реализация с Pager
- ✅ `presentation/screens/services/ServiceListViewModel.kt` - новый файл (79 строк)
- ✅ `presentation/components/ServiceCard.kt` - новый файл (165 строк)
- ✅ `presentation/screens/services/ServiceListScreen.kt` - новый файл (233 строки)
- ✅ `navigation/MainComponent.kt` - обновлен Child.Services
- ✅ `navigation/MainContent.kt` - интеграция ServiceListScreen
- ✅ `di/AppModule.kt` - добавлен ServiceListViewModel

**User Journey:** ✅ Bottom Navigation "Услуги" → Список услуг с пагинацией → ✅ Детали услуги

**Результат:** Раздел Услуг полностью готов! Товары и Услуги на одинаковом уровне детализации. 🚀

---

**🚧 Текущий этап:** Этап 11.2 - Детальная карточка услуги (ЗАВЕРШЕН! ✅)

#### ✅ Этап 11.2: Детальная карточка услуги (ServiceDetailScreen) - ЗАВЕРШЕН! 🎉

**Дата**: 2025-11-14

**Что реализовано:**

1. ✅ **ServiceDetailViewModel** - ViewModel с MVI подходом:
   - Состояния: Loading, Success(service, isOwner), Error
   - loadService() - загрузка услуги с сервера
   - Автоматическая загрузка услуги при инициализации
   - Обработка ошибок с логированием через Napier
   - Нет toggleFavorite() (у услуг нет избранного, только у товаров)

2. ✅ **ServiceDetailScreen** - UI экран деталей услуги:
   - **Галерея изображений** с HorizontalPager (свайп между фото)
   - Индикаторы страниц (dots) для навигации по фото
   - **Информация об услуге**: название, цена (или "Договорная"), описание, категория
   - **Карточка исполнителя**: имя, телефон, кнопка "Позвонить"
   - **Дополнительная информация**: просмотры, дата создания
   - **TopAppBar** с кнопкой "Назад" (без избранного)
   - Обработка всех состояний: Loading, Success, Error
   - Кнопка "Повторить" при ошибке загрузки

3. ✅ **Навигация MainComponent**:
   - Добавлен Config.ServiceDetail(serviceId: Long)
   - Добавлен Child.ServiceDetail(serviceId, onBack)
   - Child.Services теперь передает callback onServiceClick
   - Навигация через navigation.push() и navigation.pop()

4. ✅ **Интеграция в MainContent**:
   - ServiceListScreen получает onServiceClick callback
   - ServiceDetailScreen отображается при клике на услугу
   - Callback onCallProvider для звонка исполнителю (заглушка)

5. ✅ **Koin DI**:
   - Добавлен ServiceDetailViewModel с параметром serviceId
   - factory { (serviceId: Long) -> ... } для динамического создания

**Технические детали:**

- **HorizontalPager** из `androidx.compose.foundation.pager` для свайпа изображений
- **Coil** для загрузки изображений с плейсхолдерами
- **Material Design 3**: Cards, Chips, Colors, Typography
- **MVI архитектура**: единое состояние экрана (sealed interface)
- **Decompose Navigation**: push/pop для навигации между экранами
- **Симметрия с ProductDetailScreen**: аналогичная архитектура для единообразия

**Файлы созданы/изменены:**
- ✅ `ServiceDetailViewModel.kt` - новый файл (97 строк)
- ✅ `ServiceDetailScreen.kt` - новый файл (409 строк)
- ✅ `MainComponent.kt` - добавлена навигация к деталям услуги
- ✅ `MainContent.kt` - интеграция ServiceDetailScreen
- ✅ `AppModule.kt` - добавлен ServiceDetailViewModel в Koin DI

**User Journey:** ✅ Список услуг → Клик на услугу → Детали → Кнопка "Позвонить" (готова)

**TODO (следующие этапы):**
- [ ] **Android Intent для звонка**: реализовать Intent.ACTION_DIAL в androidApp (для продавцов и исполнителей)
- [ ] **Извлечение userId из JWT**: для проверки isOwner у товаров и услуг
- [ ] **Кнопка "Редактировать"**: для владельца услуги

**Результат:** Детали Услуг готовы! Товары и Услуги теперь полностью симметричны: List + Detail ✅

---

**🚧 Текущий этап:** Этап 10.2 - Экран профиля (ЗАВЕРШЕН! ✅)

#### ✅ Этап 10.2: Экран профиля (ProfileScreen) - ЗАВЕРШЕН! 🎉

**Дата**: 2025-11-14

**Что реализовано:**

1. ✅ **ProfileViewModel** - ViewModel с MVI подходом:
   - Состояния: Loading, Success(user), Error
   - loadProfile() - загрузка профиля текущего пользователя через AuthRepository.getCurrentUser()
   - logout() - выход из системы с очисткой JWT токена
   - Автоматическая загрузка профиля при инициализации
   - Обработка ошибок с логированием через Napier

2. ✅ **ProfileScreen** - UI экран профиля пользователя:
   - **Аватар**: круглый аватар пользователя или заглушка с первой буквой имени
   - **Информация о пользователе**: имя, телефон, email, биография
   - **Карточка контактов**: Material Design 3 Card с иконками (Phone, Email, Info)
   - **Кнопки действий**:
     - "Редактировать профиль" (OutlinedButton) - TODO: следующий этап
     - "Мои объявления" (OutlinedButton) - TODO: следующий этап
     - "Выйти" (Button) - работает, вызывает logout()
   - **TopAppBar** с заголовком "Профиль"
   - Обработка всех состояний: Loading, Success, Error
   - Кнопка "Повторить" при ошибке загрузки

3. ✅ **Интеграция в MainContent**:
   - Заменен HomeScreen на ProfileScreen в Child.Profile
   - Обновлены импорты
   - Bottom Navigation теперь открывает настоящий ProfileScreen

4. ✅ **Koin DI**:
   - Добавлен ProfileViewModel в viewModelModule
   - factoryOf(::ProfileViewModel) для инжекции зависимостей

**Технические детали:**

- **MVI архитектура**: единое состояние экрана (sealed interface ProfileState)
- **Material Design 3**: Cards, Buttons, Typography, Colors
- **Coil**: загрузка аватара с плейсхолдером (первая буква имени)
- **AuthRepository**: использование getCurrentUser() для получения профиля
- **TokenManager**: очистка токена при выходе

**Файлы созданы/изменены:**
- ✅ `ProfileViewModel.kt` - новый файл (85 строк)
- ✅ `ProfileScreen.kt` - новый файл (352 строки)
- ✅ `AppModule.kt` - добавлен ProfileViewModel в Koin DI
- ✅ `MainContent.kt` - заменен HomeScreen на ProfileScreen

**User Journey:** ✅ Bottom Navigation "Профиль" → Экран профиля с данными пользователя → Кнопка "Выйти"

**TODO (следующие этапы):**
- [ ] **EditProfileScreen** - редактирование профиля (имя, биография, аватар)
- [ ] **MyProductsScreen** - список товаров текущего пользователя
- [ ] **Android Intent для звонка**: реализовать Intent.ACTION_DIAL в androidApp (для продавцов и исполнителей)
- [ ] **Извлечение userId из JWT**: для проверки isOwner у товаров и услуг

**Результат:** Экран профиля готов! Bottom Navigation полностью функционален (Товары, Услуги, Профиль) ✅

---

**🚧 Следующий этап:** Этап 10.3 - Редактирование профиля или Этап 12 - Создание объявлений

**Варианты следующих задач:**

**Вариант А: Редактирование профиля (Этап 10.3)**
1. **EditProfileScreen** - форма редактирования профиля
2. **EditProfileViewModel** - управление состоянием редактирования
3. **Загрузка аватара** - выбор фото из галереи/камеры
4. **Навигация** - добавить Config.EditProfile в MainComponent

**Вариант Б: Создание объявлений (Этап 12)**
1. **CreateProductScreen** - форма создания товара
2. **CreateServiceScreen** - форма создания услуги
3. **Загрузка фотографий** - выбор нескольких фото
4. **Выбор категории** - интеграция с CategoryRepository

**Вариант В: Мои объявления (Этап 10.4)**
1. **MyProductsScreen** - список товаров текущего пользователя
2. **Кнопки "Редактировать" и "Удалить"** для каждого товара
3. **MyServicesScreen** - список услуг текущего пользователя

---

**Backend улучшения (после Mobile MVP):**
- Rate Limiting middleware
- JWT Secret в environment variables
- CORS для specific origins
- Улучшенный Health Check (с проверкой БД)
- Graceful Shutdown
- Unit и Integration тесты

**✅ ЗАВЕРШЕНО: Общий модуль API моделей (2025-11-10)**

**Проблема (решена):**
API модели дублировались между сервером и клиентом, что приводило к рассинхронизации и ошибкам десериализации.

**Решение: Создан отдельный модуль `api-models`** ✅

```
SocialSpace/
├── api-models/              # ✅ Единый источник истины для API моделей
│   ├── build.gradle.kts     # ✅ Kotlin Multiplatform (JVM + Android + iOS)
│   └── src/commonMain/kotlin/info/javaway/sc/api/models/
│       ├── User.kt          # ✅ User, UserRole, UserPublicInfo, PublicUserProfile
│       ├── UserDto.kt       # ✅ RegisterRequest, LoginRequest, AuthResponse, UpdateProfileRequest
│       ├── Product.kt       # ✅ Product, ProductCondition, ProductStatus
│       ├── ProductDto.kt    # ✅ CreateProductRequest, UpdateProductRequest, ProductResponse, ProductListItem, ProductListResponse
│       ├── Category.kt      # ✅ Category, CategoryType, CategoryInfo
│       ├── Service.kt       # ✅ Service, ServiceStatus
│       ├── ServiceDto.kt    # ✅ CreateServiceRequest, UpdateServiceRequest, ServiceResponse, ServiceListResponse
│       └── ApiResponse.kt   # ✅ ErrorResponse, SuccessResponse, FileUploadResponse
├── backend/                 # ✅ Использует api-models
│   └── build.gradle.kts     # ✅ implementation(project(":api-models"))
├── sharedUI/                # ✅ Использует api-models
│   └── build.gradle.kts     # ✅ implementation(project(":api-models"))
└── androidApp/
```

**Что сделано:**
1. ✅ Создан модуль `api-models` с Kotlin Multiplatform (JVM + Android + iOS)
2. ✅ Перенесены все DTO модели из backend в api-models (8 файлов)
3. ✅ Удалены дублирующиеся модели из backend/models/ и sharedUI/domain/models/
4. ✅ Обновлены импорты в backend (15 файлов): `info.javaway.sc.backend.models.*` → `info.javaway.sc.api.models.*`
5. ✅ Обновлены импорты в sharedUI (14 файлов): `info.javaway.sc.shared.domain.models.*` → `info.javaway.sc.api.models.*`
6. ✅ Добавлены зависимости в backend и sharedUI на модуль api-models
7. ✅ Синтаксис проверен вручную - всё корректно

**Преимущества (достигнуты):**
- ✅ Единый источник истины - модели определены в одном месте
- ✅ Компилятор не даст рассинхронизировать модели
- ✅ Легко переиспользовать в других клиентах (iOS, Web, Desktop)
- ✅ Проще поддерживать и развивать API
- ✅ Нет дублирования кода

---
Вместо import kotlinx.datetime.Clock всегда используй  kotlin.time.Clock
Для сетевого обмена необходимо использовать СТРОГО только модели из общего модуля api-models
Разделяй мобильное приложение на слои data, domain, ui
В качестве состояния экранов используй MVI подходи с единым состоянием:
    sealed interface State { Loading, Error, Data и так далее, в зависимости от фичи }
Ни в коем случае не используй на вью слое модели из info.javaway.sc.api
Для отображения изображений используй AsyncImage Coil

---

**🚧 Текущий этап:** Этап 12 - Создание объявлений (100% ЗАВЕРШЕНО! ✅)

#### ✅ Этап 12.1: Создание товаров (CreateProductScreen) - ЗАВЕРШЕНО! 🎉

**Дата**: 2025-11-14

**Что реализовано:**

1. ✅ **ImagePicker (expect/actual)** - multiplatform выбор изображений:
   - `ImagePicker.kt` (commonMain) - data class SelectedImage + expect функция rememberImagePickerLauncher
   - `ImagePicker.android.kt` (androidMain) - actual реализация через ActivityResultContracts.PickMultipleVisualMedia
   - Поддержка до 5 изображений одновременно
   - Автоматическое чтение ByteArray, fileName, mimeType из Uri

2. ✅ **CategorySelector UI компонент**:
   - `CategorySelectorField` - OutlinedTextField с выпадающим диалогом
   - Фильтрация категорий по типу (PRODUCT/SERVICE)
   - Material Design 3 дизайн с иконками категорий
   - Переиспользуемый для товаров и услуг

3. ✅ **ApiClient методы для создания и загрузки**:
   - `uploadImage()` - загрузка одного изображения через Multipart/form-data
   - `uploadImages()` - пакетная загрузка нескольких изображений
   - `createProduct()` - создание товара (POST /api/products)
   - `updateProduct()` - обновление товара (PUT /api/products/{id})
   - `deleteProduct()` - удаление товара (DELETE /api/products/{id})
   - `createService()` - создание услуги (POST /api/services)
   - `updateService()` / `deleteService()` - CRUD для услуг

4. ✅ **CreateProductViewModel** - ViewModel с MVI архитектурой:
   - Состояния: Loading, Form, Creating, Success(productId), Error
   - ProductFormState - состояние формы (title, description, price, category, condition, images)
   - Методы обновления: updateTitle(), updateDescription(), updatePrice(), selectCategory(), selectCondition()
   - selectImages() - добавление до 5 изображений с валидацией
   - removeImage(index) - удаление изображения по индексу
   - createProduct() - создание товара с валидацией и загрузкой изображений
   - Валидация: название (1-200 символов), описание (обязательное), цена (>= 0), категория, изображения (1-5)

5. ✅ **CreateProductScreen** - UI экран создания товара:
   - Scaffold с TopAppBar (кнопка "Назад", заголовок "Новый товар")
   - Форма с полями: название, описание, цена, категория, состояние (NEW/USED)
   - FilterChip для выбора состояния товара
   - ImageSelector - компонент для управления изображениями
   - Галерея выбранных изображений с предпросмотром (Skia Image → ImageBitmap)
   - Кнопка "Добавить фото" (max 5 изображений)
   - Кнопка "Создать товар" с индикатором загрузки
   - Обработка состояний: Loading, Form, Creating, Success, Error

6. ✅ **Навигация CreateProduct**:
   - Обновлен `MainComponent`: добавлены Config.CreateProduct, Child.CreateProduct
   - Child.Products теперь имеет callback onCreateProduct
   - После создания товара: навигация на детали товара (Config.ProductDetail)
   - Обновлен `MainContent`: интеграция CreateProductScreen с koinInject

7. ✅ **FloatingActionButton на списках**:
   - `ProductListScreen` - FAB "+" для создания товара
   - `ServiceListScreen` - FAB "+" для создания услуги (TODO: CreateServiceScreen)
   - Scaffold обертка с floatingActionButton параметром
   - Material Design 3 Icons.Default.Add

8. ✅ **Koin DI**:
   - Добавлен CreateProductViewModel в viewModelModule
   - factoryOf(::CreateProductViewModel)

**Технические детали:**

- **Expect/Actual**: паттерн для multiplatform кода (ImagePicker)
- **Multipart/form-data**: загрузка файлов через Ktor Client
- **Skia Image**: декодирование ByteArray в ImageBitmap для предпросмотра
- **MVI архитектура**: единое состояние экрана (sealed interface)
- **Material Design 3**: Scaffold, OutlinedTextField, FilterChip, FloatingActionButton
- **Валидация**: клиентская валидация перед отправкой на сервер
- **Decompose Navigation**: push/pop для навигации

**Файлы созданы:**
- ✅ `ImagePicker.kt` (commonMain) - expect класс (43 строки)
- ✅ `ImagePicker.android.kt` (androidMain) - actual реализация (106 строк)
- ✅ `CategorySelector.kt` - UI компонент (180 строк)
- ✅ `CreateProductViewModel.kt` - ViewModel (322 строки)
- ✅ `CreateProductScreen.kt` - UI экран (485 строк)

**Файлы изменены:**
- ✅ `ApiClient.kt` - добавлены методы uploadImage(), uploadImages(), createProduct(), updateProduct(), deleteProduct(), createService(), updateService(), deleteService() (~90 новых строк)
- ✅ `MainComponent.kt` - добавлена навигация CreateProduct и CreateService
- ✅ `MainContent.kt` - интеграция CreateProductScreen
- ✅ `ProductListScreen.kt` - добавлен FloatingActionButton
- ✅ `ServiceListScreen.kt` - добавлен FloatingActionButton
- ✅ `AppModule.kt` - добавлен CreateProductViewModel в Koin DI

**User Journey:** ✅ Список товаров → FAB "+" → Форма создания → Загрузка изображений → Создание → Детали товара

**Результат:** Создание товаров полностью готово! Пользователи могут создавать объявления с фотографиями! 🚀

---

#### ✅ Этап 12.2: Создание услуг (CreateServiceScreen) - ЗАВЕРШЕНО! 🎉

**Дата**: 2025-11-14

**Что реализовано:**

1. ✅ **CreateServiceViewModel** - ViewModel с MVI архитектурой:
   - Состояния: Loading, Form, Creating, Success(serviceId), Error
   - ServiceFormState - состояние формы (title, description, price, isNegotiable, category, images)
   - Методы обновления: updateTitle(), updateDescription(), updatePrice(), toggleNegotiablePrice(), selectCategory()
   - selectImages() - добавление до 5 изображений с валидацией
   - removeImage(index) - удаление изображения по индексу
   - createService() - создание услуги с валидацией и загрузкой изображений
   - Валидация: название (1-200 символов), описание (обязательное), цена (опциональная, пропускается если isNegotiable), категория, изображения (1-5)
   - **Отличия от Product**: нет поля `condition`, есть поле `isNegotiable` (Checkbox "Договорная цена")

2. ✅ **CreateServiceScreen** - UI экран создания услуги:
   - Scaffold с TopAppBar (кнопка "Назад", заголовок "Новая услуга")
   - Форма с полями: название, описание, цена (опциональная), категория
   - **Checkbox "Договорная цена"** - отключает поле цены
   - ImageSelector - компонент для управления изображениями (переиспользуется из CreateProductScreen)
   - Галерея выбранных изображений с предпросмотром
   - Кнопка "Добавить фото примеров работ" (max 5 изображений)
   - Кнопка "Создать услугу" с индикатором загрузки
   - Обработка состояний: Loading, Form, Creating, Success, Error
   - **Отличия от Product**: нет FilterChip для состояния (NEW/USED), есть Checkbox для "Договорная цена"

3. ✅ **Интеграция в MainContent**:
   - Заменен PlaceholderScreen на реальный CreateServiceScreen
   - koinInject<CreateServiceViewModel>() для получения ViewModel
   - Callback onSuccess навигирует на детали услуги (Config.ServiceDetail)

4. ✅ **Koin DI**:
   - Добавлен CreateServiceViewModel в viewModelModule
   - factoryOf(::CreateServiceViewModel)

5. ✅ **Удален PlaceholderScreen**:
   - PlaceholderScreen больше не используется, удален из MainContent.kt

**Технические детали:**

- **CategoryType.SERVICE**: фильтрация категорий для услуг через CategorySelectorField
- **Опциональная цена**: поле `price` может быть null (CreateServiceRequest)
- **isNegotiable**: UI логика для отключения поля цены при включенном Checkbox
- **Симметрия с CreateProductScreen**: аналогичная архитектура для единообразия кода

**Файлы созданы:**
- ✅ `CreateServiceViewModel.kt` - новый файл (304 строки)
- ✅ `CreateServiceScreen.kt` - новый файл (446 строк)

**Файлы изменены:**
- ✅ `MainContent.kt` - интеграция CreateServiceScreen, удален PlaceholderScreen
- ✅ `AppModule.kt` - добавлен CreateServiceViewModel в Koin DI

**User Journey:** ✅ Список услуг → FAB "+" → Форма создания → Загрузка изображений → Создание → Детали услуги

**Результат:** **Этап 12 полностью завершен!** Пользователи могут создавать объявления как о товарах, так и об услугах! 🎉

---

### 🐛 Исправлен баг: Формат ответа POST/PUT /api/services (2025-11-14)

**Проблема:**
При создании и обновлении услуг возникала ошибка десериализации:
```
JsonConvertException: Fields [service, user, category] are required for type 'ServiceResponse', but they were missing
```

**Причина:**
Backend возвращал просто объект `Service` вместо `ServiceResponse` (который содержит service + user + category).

**Исправление:**

1. ✅ **POST /api/services** (ServiceRoutes.kt:364-401):
   - Было: `call.respond(HttpStatusCode.Created, service)`
   - Стало: создается `ServiceResponse` с загрузкой user и category, аналогично ProductRoutes

2. ✅ **PUT /api/services/{id}** (ServiceRoutes.kt:498-536):
   - Было: `call.respond(HttpStatusCode.OK, updatedService)`
   - Стало: создается `ServiceResponse` с загрузкой user и category

**Файлы изменены:**
- ✅ `backend/src/main/kotlin/info/javaway/sc/backend/routes/ServiceRoutes.kt` - POST и PUT endpoints

**Результат:** Создание и обновление услуг работает корректно! Backend возвращает полный ServiceResponse. ✅

---

**🚧 Текущий этап:** Этап 13 - Управление своими объявлениями (My Items Management)

#### ✅ Этап 13.1: Определение владельца объявления (isOwner) - ЗАВЕРШЕН! 🎉

**Дата**: 2025-11-14

**Что реализовано:**

1. ✅ **ProductDetailViewModel** - определение isOwner:
   - Заменен `TokenManager` на `AuthRepository` в конструкторе
   - Используется `authRepository.getUserId()` для получения текущего userId
   - Сравнение `currentUserId == product.user.id` для определения владельца
   - Добавлено логирование через Napier для отладки

2. ✅ **ServiceDetailViewModel** - определение isOwner:
   - Заменен `TokenManager` на `AuthRepository` в конструкторе
   - Используется `authRepository.getUserId()` для получения текущего userId
   - Сравнение `currentUserId == service.user.id` для определения владельца
   - Добавлено логирование через Napier для отладки

3. ✅ **Koin DI (AppModule)** - обновлены зависимости:
   - ProductDetailViewModel теперь получает `authRepository` вместо `tokenManager`
   - ServiceDetailViewModel теперь получает `authRepository` вместо `tokenManager`

4. ✅ **ProductDetailScreen** - кнопка "Редактировать":
   - Добавлены импорты: `Icons.Default.Edit`, `OutlinedButton`
   - Обновлена сигнатура `ProductDetailContent`: добавлены `isOwner` и `onEditProduct`
   - Кнопка "Редактировать объявление" отображается только владельцу (`if (isOwner)`)
   - Кнопка размещена перед `SellerCard` для удобства UX
   - TODO: навигация на `EditProductScreen` (следующий этап)

5. ✅ **ServiceDetailScreen** - кнопка "Редактировать":
   - Добавлены импорты: `Icons.Default.Edit`, `OutlinedButton`
   - Обновлена сигнатура `ServiceDetailContent`: добавлены `isOwner` и `onEditService`
   - Кнопка "Редактировать объявление" отображается только владельцу (`if (isOwner)`)
   - Кнопка размещена перед `ProviderCard` для удобства UX
   - TODO: навигация на `EditServiceScreen` (следующий этап)

**Технические детали:**

- **AuthRepository.getUserId()**: метод уже существовал в интерфейсе, делегирует к `TokenManager.getUserId()`
- **TokenManager.saveUserId()**: userId сохраняется при login и register в `AuthRepositoryImpl`
- **MVI архитектура**: состояние `isOwner` находится в Success state (ProductDetailState.Success, ServiceDetailState.Success)
- **Material Design 3**: OutlinedButton для вторичного действия, иконка Edit для визуальной ясности

**Файлы изменены:**
- ✅ `ProductDetailViewModel.kt` - AuthRepository вместо TokenManager, определение isOwner
- ✅ `ServiceDetailViewModel.kt` - AuthRepository вместо TokenManager, определение isOwner
- ✅ `AppModule.kt` - обновлены factory для ProductDetailViewModel и ServiceDetailViewModel
- ✅ `ProductDetailScreen.kt` - добавлена кнопка "Редактировать" для владельца
- ✅ `ServiceDetailScreen.kt` - добавлена кнопка "Редактировать" для владельца

**User Journey:** ✅ Детали товара/услуги → Видит кнопку "Редактировать" (если владелец) → Клик → TODO: EditScreen

**Результат:** **Этап 13.1 завершен!** Владелец объявления может видеть кнопку "Редактировать". Готов фундамент для редактирования и управления объявлениями! 🚀

---

#### ✅ Этап 13.2: My Products Screen (Мои товары) - ЗАВЕРШЕН! 🎉

**Дата**: 2025-11-14

**Что реализовано:**

1. ✅ **MyProductsViewModel** - ViewModel с MVI архитектурой:
   - Состояния: Loading, Success(products), Empty, Error
   - loadMyProducts() - загрузка списка своих товаров через ProductRepository.getMyProducts()
   - deleteProduct(productId) - удаление товара с оптимистичным обновлением UI
   - Откат изменений при ошибке удаления
   - Обработка пустого списка (Empty state)

2. ✅ **MyProductsScreen** - UI экран списка своих товаров:
   - TopAppBar с кнопкой "Назад" и заголовком "Мои объявления"
   - LazyColumn со списком своих товаров
   - MyProductCard - карточка с кнопками управления:
     - Переиспользование ProductCard для отображения товара
     - Кнопка "Редактировать" → onEditProduct callback
     - Кнопка "Удалить" → диалог подтверждения → deleteProduct()
   - DeleteConfirmationDialog - модальное окно подтверждения удаления
   - EmptyContent - красивый экран для пустого списка с кнопкой "Создать объявление"
   - FloatingActionButton "+" для создания нового товара
   - Обработка всех состояний: Loading, Success, Empty, Error

3. ✅ **Навигация (MainComponent)**:
   - Добавлен Config.MyProducts (Serializable)
   - Добавлен Child.MyProducts с callbacks (onBack, onProductClick, onEditProduct, onCreateProduct)
   - Child.Profile теперь имеет callback onMyProductsClick
   - Навигация: Profile → MyProducts → ProductDetail / CreateProduct

4. ✅ **Интеграция в ProfileScreen**:
   - Обновлена сигнатура ProfileScreen: добавлен параметр onMyProductsClick
   - Кнопка "Мои объявления" теперь вызывает onMyProductsClick вместо println
   - Callback передается через ProfileContent

5. ✅ **MainContent**:
   - Добавлен импорт MyProductsScreen
   - Добавлен case для Child.MyProducts в when expression
   - MyProductsScreen отображается при навигации на Config.MyProducts

6. ✅ **ProductRepository**:
   - Добавлен метод deleteProduct(productId: Long): Result<Unit> в интерфейс
   - Реализован deleteProduct() в ProductRepositoryImpl через ApiClient

7. ✅ **Koin DI**:
   - Добавлен MyProductsViewModel в viewModelModule
   - factoryOf(::MyProductsViewModel)

**Технические детали:**

- **MVI архитектура**: единое состояние экрана (sealed interface MyProductsState)
- **Оптимистичное UI**: товар удаляется из списка сразу, откат при ошибке
- **Material Design 3**: AlertDialog для подтверждения удаления
- **Переиспользование компонентов**: ProductCard внутри MyProductCard
- **Decompose Navigation**: интеграция с существующей навигационной системой

**Файлы созданы:**
- ✅ `MyProductsViewModel.kt` - новый файл (122 строки)
- ✅ `MyProductsScreen.kt` - новый файл (397 строк)

**Файлы изменены:**
- ✅ `MainComponent.kt` - добавлены Config.MyProducts и Child.MyProducts
- ✅ `ProfileScreen.kt` - добавлен параметр onMyProductsClick
- ✅ `MainContent.kt` - интеграция MyProductsScreen
- ✅ `ProductRepository.kt` - добавлен метод deleteProduct()
- ✅ `ProductRepositoryImpl.kt` - реализация deleteProduct()
- ✅ `AppModule.kt` - добавлен MyProductsViewModel в Koin DI

**User Journey:** ✅ Профиль → "Мои объявления" → Список своих товаров → "Редактировать" (TODO) / "Удалить" (работает)

**Результат:** **Этап 13.2 завершен!** Пользователи могут просматривать свои товары, удалять их, создавать новые и переходить к деталям! 🚀

---

#### ✅ Этап 13.3: Edit Product Screen (Редактирование товара) - ЗАВЕРШЕН! 🎉

**Дата**: 2025-11-14

**Что реализовано:**

1. ✅ **EditProductViewModel** - ViewModel с MVI архитектурой:
   - Состояния: Loading, Form, Updating, Success(productId), Error
   - EditProductFormState - расширенная форма с управлением изображениями:
     - existingImages: List<String> - URL существующих изображений
     - newImages: List<SelectedImage> - новые выбранные изображения
     - removedExistingImageIndices: Set<Int> - индексы удаленных изображений
   - loadProduct() - загрузка существующего товара и категорий, предзаполнение формы
   - updateProduct() - обновление товара с валидацией и загрузкой новых изображений
   - Методы управления изображениями:
     - selectImages() - добавление новых изображений (до 5 общих)
     - removeExistingImage(index) - пометка существующего изображения для удаления
     - removeNewImage(index) - удаление нового изображения
   - Валидация: название (1-200), описание, цена (>= 0), категория, изображения (1-5)

2. ✅ **EditProductScreen** - UI экран редактирования:
   - TopAppBar с заголовком "Редактирование товара"
   - Форма с предзаполненными данными из существующего товара
   - Поля: название, описание, цена, категория, состояние (NEW/USED)
   - **EditImageSelector** - управление двумя типами изображений:
     - ExistingImageCard - карточки существующих изображений (с Coil AsyncImage)
     - NewImagePreviewCard - карточки новых изображений (локальные файлы)
     - Возможность удалять существующие и новые изображения независимо
   - Кнопка "Сохранить изменения" с индикатором загрузки
   - Обработка всех состояний: Loading, Form, Updating, Success, Error

3. ✅ **Навигация (MainComponent)**:
   - Добавлен Config.EditProduct(productId: Long)
   - Добавлен Child.EditProduct(productId, onBack, onSuccess)
   - MyProducts: onEditProduct callback теперь навигирует на EditProductScreen
   - ProductDetail: добавлен onEditProduct callback (если isOwner)
   - После обновления: навигация на детали товара (Config.ProductDetail)

4. ✅ **Интеграция в MainContent**:
   - Добавлен импорт EditProductScreen
   - koinInject<EditProductViewModel> { parametersOf(productId) }
   - Обработка Child.EditProduct в when expression

5. ✅ **ProductDetailScreen обновлен**:
   - Добавлен параметр onEditProduct: ((Long) -> Unit)? = null
   - Кнопка "Редактировать" теперь вызывает callback вместо println
   - Callback передается из MainComponent через Child.ProductDetail

6. ✅ **Koin DI**:
   - Добавлен EditProductViewModel в viewModelModule
   - factory { (productId: Long) -> EditProductViewModel(...) }

**Технические детали:**

- **Dual Image Management**: раздельное управление существующими (URL) и новыми (SelectedImage) изображениями
- **Optimistic Upload**: только новые изображения загружаются на сервер, существующие остаются по URL
- **UpdateProductRequest**: все поля опциональные, отправляются только измененные данные
- **Coil AsyncImage**: для предпросмотра существующих изображений с сервера
- **MVI архитектура**: единое состояние экрана (sealed interface EditProductState)
- **Decompose Navigation**: интеграция с существующей системой навигации

**Файлы созданы:**
- ✅ `EditProductViewModel.kt` - новый файл (360 строк)
- ✅ `EditProductScreen.kt` - новый файл (500+ строк)

**Файлы изменены:**
- ✅ `MainComponent.kt` - добавлены Config.EditProduct и Child.EditProduct, обновлен ProductDetail и MyProducts
- ✅ `MainContent.kt` - интеграция EditProductScreen
- ✅ `ProductDetailScreen.kt` - добавлен параметр onEditProduct
- ✅ `AppModule.kt` - добавлен EditProductViewModel с параметром productId

**User Journey:** ✅ Мои объявления → Клик "Редактировать" → Форма с данными → Редактирование → Сохранение → Детали товара

**Результат:** **Этап 13.3 завершен!** CRUD для товаров полностью реализован (Create ✅, Read ✅, Update ✅, Delete ✅). Пользователи могут редактировать свои объявления! 🚀

---

#### ✅ Этап 13.4: My Services + Edit Service (Управление услугами) - ЗАВЕРШЕН! 🎉

**Дата**: 2025-11-14

**Что реализовано:**

1. ✅ **MyServicesViewModel** - ViewModel с MVI архитектурой:
   - Состояния: Loading, Success(services), Empty, Error
   - loadMyServices() - загрузка списка своих услуг через ServiceRepository.getMyServices()
   - deleteService(serviceId) - удаление услуги с оптимистичным обновлением UI
   - Откат изменений при ошибке удаления
   - Обработка пустого списка (Empty state)

2. ✅ **MyServicesScreen** - UI экран списка своих услуг:
   - TopAppBar с кнопкой "Назад" и заголовком "Мои услуги"
   - LazyColumn со списком своих услуг
   - MyServiceCard - карточка с кнопками управления:
     - Переиспользование ServiceCard для отображения услуги
     - Кнопка "Редактировать" → onEditService callback
     - Кнопка "Удалить" → диалог подтверждения → deleteService()
   - DeleteConfirmationDialog - модальное окно подтверждения удаления
   - EmptyContent - экран для пустого списка с кнопкой "Создать объявление"
   - FloatingActionButton "+" для создания новой услуги
   - Обработка всех состояний: Loading, Success, Empty, Error

3. ✅ **ServiceRepository обновлен**:
   - Добавлен метод deleteService(serviceId: Long): Result<Unit> в интерфейс
   - Реализован deleteService() в ServiceRepositoryImpl через ApiClient
   - getMyServices() уже существовал

4. ✅ **EditServiceViewModel** - ViewModel с MVI архитектурой:
   - Состояния: Loading, Form, Updating, Success(serviceId), Error
   - EditServiceFormState - расширенная форма с управлением изображениями:
     - existingImages: List<String> - URL существующих изображений
     - newImages: List<SelectedImage> - новые выбранные изображения
     - removedExistingImageIndices: Set<Int> - индексы удаленных изображений
     - isNegotiable: Boolean - флаг "Договорная цена"
   - loadService() - загрузка существующей услуги и категорий, предзаполнение формы
   - updateService() - обновление услуги с валидацией и загрузкой новых изображений
   - Методы управления изображениями: selectImages(), removeExistingImage(), removeNewImage()
   - Валидация: название (1-200), описание, цена (опциональная, пропускается если isNegotiable), категория, изображения (1-5)
   - **Отличия от EditProductViewModel**: нет поля condition, есть isNegotiable для "Договорной цены"

5. ✅ **EditServiceScreen** - UI экран редактирования услуги:
   - TopAppBar с заголовком "Редактирование услуги"
   - Форма с предзаполненными данными из существующей услуги
   - Поля: название, описание, цена (опциональная), категория
   - **Checkbox "Договорная цена"** - отключает поле цены при включении
   - **EditImageSelector** - управление двумя типами изображений (аналогично EditProductScreen)
   - Кнопка "Сохранить изменения" с индикатором загрузки
   - Обработка всех состояний: Loading, Form, Updating, Success, Error
   - **Отличия от EditProductScreen**: нет FilterChip для состояния (NEW/USED), есть Checkbox для "Договорная цена"

6. ✅ **Навигация (MainComponent)**:
   - Добавлены Config.MyServices и Config.EditService(serviceId: Long)
   - Добавлены Child.MyServices и Child.EditService
   - Child.Profile: добавлен callback onMyServicesClick
   - Child.ServiceDetail: добавлен callback onEditService
   - Навигация: Profile → MyServices → ServiceDetail / EditService / CreateService

7. ✅ **Интеграция в MainContent**:
   - Добавлены импорты MyServicesScreen и EditServiceScreen
   - Интеграция MyServicesScreen в when expression
   - koinInject<EditServiceViewModel> { parametersOf(serviceId) } для EditServiceScreen
   - Обработка Child.MyServices и Child.EditService

8. ✅ **ProfileScreen обновлен**:
   - Добавлен параметр onMyServicesClick
   - Кнопка "Мои услуги" вызывает callback для навигации на MyServicesScreen

9. ✅ **ServiceDetailScreen обновлен**:
   - Добавлен параметр onEditService: ((Long) -> Unit)? = null
   - Кнопка "Редактировать объявление" отображается только владельцу (if isOwner && onEditService != null)
   - Callback передается из MainComponent через Child.ServiceDetail

10. ✅ **Koin DI**:
    - Добавлен MyServicesViewModel в viewModelModule: factoryOf(::MyServicesViewModel)
    - Добавлен EditServiceViewModel с параметром serviceId: factory { (serviceId: Long) -> EditServiceViewModel(...) }

**Технические детали:**

- **Симметрия с Products**: Services теперь имеют полный CRUD (Create ✅, Read ✅, Update ✅, Delete ✅)
- **Dual Image Management**: раздельное управление существующими (URL) и новыми (SelectedImage) изображениями
- **Optimistic Upload**: только новые изображения загружаются на сервер
- **UpdateServiceRequest**: все поля опциональные, отправляются только измененные данные
- **MVI архитектура**: единое состояние экрана (sealed interface)
- **Material Design 3**: AlertDialog, Checkbox, Cards, Buttons
- **Decompose Navigation**: интеграция с существующей системой навигации

**Файлы созданы:**
- ✅ `MyServicesViewModel.kt` - новый файл (127 строк)
- ✅ `MyServicesScreen.kt` - новый файл (397 строк)
- ✅ `EditServiceViewModel.kt` - новый файл (367 строк)
- ✅ `EditServiceScreen.kt` - новый файл (496 строк)

**Файлы изменены:**
- ✅ `ServiceRepository.kt` - добавлен метод deleteService()
- ✅ `ServiceRepositoryImpl.kt` - реализация deleteService()
- ✅ `MainComponent.kt` - добавлены Config/Child для MyServices и EditService, обновлены Profile и ServiceDetail
- ✅ `MainContent.kt` - интеграция MyServicesScreen и EditServiceScreen
- ✅ `ProfileScreen.kt` - добавлен параметр onMyServicesClick и кнопка "Мои услуги"
- ✅ `ServiceDetailScreen.kt` - добавлен параметр onEditService
- ✅ `AppModule.kt` - добавлены MyServicesViewModel и EditServiceViewModel в Koin DI

**User Journey:** ✅ Профиль → "Мои услуги" → Список → "Редактировать" / "Удалить" → Сохранение → Детали услуги

**Результат:** **Этап 13.4 завершен!** Архитектурная симметрия достигнута! Товары и Услуги имеют полный CRUD функционал:
- **Products**: Create ✅, Read ✅, Update ✅, Delete ✅
- **Services**: Create ✅, Read ✅, Update ✅, Delete ✅

Пользователи могут полноценно управлять своими объявлениями как о товарах, так и об услугах! 🎉🚀

---

---

#### ✅ Этап 15: Android Intent для звонка (ЗАВЕРШЕН! 🎉)

**Дата**: 2025-11-15

**Что реализовано:**

1. ✅ **PhoneDialer (expect/actual)** - multiplatform утилита для звонков:
   - `PhoneDialer.kt` (commonMain) - expect class с методом dial(phoneNumber: String)
   - `PhoneDialer.android.kt` (androidMain) - actual реализация через Intent.ACTION_DIAL
   - Нормализация номера телефона (удаление пробелов, дефисов, скобок)
   - Проверка наличия приложения для обработки Intent
   - Логирование через Napier для отладки
   - **ACTION_DIAL vs ACTION_CALL**: используется ACTION_DIAL (не требует разрешения CALL_PHONE)

2. ✅ **Koin DI интеграция**:
   - Создан `PlatformModule.android.kt` - actual реализация platformModule
   - PhoneDialer инжектится с Android Context через `androidContext()`
   - Добавлен platformModule в appModules (AppModule.kt)

3. ✅ **MainContent обновлен**:
   - PhoneDialer инжектируется через `koinInject<PhoneDialer>()`
   - ProductDetailScreen: onCallSeller теперь вызывает `phoneDialer.dial(phone)`
   - ServiceDetailScreen: onCallProvider теперь вызывает `phoneDialer.dial(phone)`
   - Удалены заглушки `println("Call seller: $phone")` и `println("Call provider: $phone")`

**Технические детали:**

- **Intent.ACTION_DIAL**: открывает набор номера с предзаполненным номером, пользователь нажимает "Позвонить"
- **Нормализация номера**: `phoneNumber.replace(Regex("[\\s\\-()]"), "")` для корректного формата
- **FLAG_ACTIVITY_NEW_TASK**: необходим для запуска Activity из Context, который не является Activity
- **resolveActivity()**: проверка наличия приложения для обработки Intent (безопасность)
- **Expect/Actual паттерн**: позволяет добавить iOS реализацию в будущем

**Файлы созданы:**
- ✅ `PhoneDialer.kt` (commonMain) - expect класс (14 строк)
- ✅ `PhoneDialer.android.kt` (androidMain) - actual реализация (41 строка)
- ✅ `PlatformModule.android.kt` - Koin модуль для Android (14 строк)

**Файлы изменены:**
- ✅ `AppModule.kt` - добавлен импорт PhoneDialer, expect platformModule, включен в appModules
- ✅ `MainContent.kt` - добавлен импорт PhoneDialer, koinInject, замены заглушек на phoneDialer.dial()

**User Journey:** ✅ Детали товара/услуги → Кнопка "Позвонить" → Открывается набор номера Android → Звонок продавцу/исполнителю

**Результат:** **Этап 15 завершен!** Кнопки "Позвонить" полностью функциональны. Пользователи могут связаться с продавцами и исполнителями одним кликом! 🚀📞

---

---

## 🚀 Новая концепция: ПУЛЬСАР - Платформа для жилищных комплексов

**Дата обновления**: 2025-11-16

### Ребрендинг и новая архитектура:
- **Название приложения**: Пульсар (вместо SocialSpace)
- **Слоган**: "Пульсар - в ритме твоего пространства"
- **Концепция**: Multi-tenant платформа для жилищных комплексов

### Ключевая фича: Пространства (Spaces)
Каждое пространство = отдельный жилищный комплекс с:
- Своими объявлениями (товары, услуги)
- Лентой новостей
- Документацией (устав, правила, договоры)
- Списком участников
- Приватностью (публичное/приватное)

### Новые требования:
1. ✅ **Концепция Пространств** - создание/присоединение к пространствам (жилищным комплексам)
2. 🎨 **Тёмная/светлая тема** - переключение темы оформления
3. 🎨 **Улучшение светлой темы** - современный дизайн
4. 🔐 **Вход по email** - альтернативный способ входа (в дополнение к телефону)
5. 📰 **Лента новостей** - новости для каждого пространства
6. 📄 **Раздел документации** - важные документы пространства
7. 💬 **Чат между пользователями** - переписка

---

## 📋 Обновленный план развития (2025-11-16)

### **🔴 ПРИОРИТЕТ 1: Фаза "Пространства" (Spaces Architecture) - КРИТИЧНО! (2-3 недели)**

> **ПОЧЕМУ СЕЙЧАС:** Концепция Пространств меняет всю архитектуру приложения (модели данных, БД, API, навигация). Это нужно реализовать **ДО** запуска в production, иначе миграция будет крайне болезненной.

#### Этап 16: Backend - Space модель и миграция БД (3-5 дней)
**Цель:** Создать модель пространства и мигрировать существующие данные

1. **Создание таблиц БД** (Exposed):
   - `Spaces` таблица:
     - id: Long (PK)
     - name: String - название пространства (например "ЖК Солнечный")
     - slug: String - уникальный URL-friendly идентификатор (например "jk-solnechnyj")
     - description: String? - описание пространства
     - logo: String? - логотип пространства (URL)
     - type: SpaceType - PUBLIC (публичное, любой может присоединиться) / PRIVATE (по приглашению)
     - inviteCode: String? - код приглашения для приватных пространств
     - createdAt: Instant
     - ownerId: Long (FK to Users) - создатель пространства

   - `SpaceMembers` таблица:
     - id: Long (PK)
     - spaceId: Long (FK to Spaces)
     - userId: Long (FK to Users)
     - role: SpaceMemberRole - OWNER, ADMIN, MEMBER
     - joinedAt: Instant
     - Unique index: (spaceId, userId)

2. **Миграция существующих таблиц**:
   - Products: добавить `spaceId: Long` (FK to Spaces, NOT NULL)
   - Services: добавить `spaceId: Long` (FK to Spaces, NOT NULL)
   - Users: добавить `defaultSpaceId: Long?` (FK to Spaces, nullable) - текущее активное пространство

3. **Seed данные**:
   - Создать пространство "Demo Space" по умолчанию
   - Перенести все существующие Products/Services в "Demo Space"
   - Добавить всех пользователей в "Demo Space" как MEMBER

4. **api-models модуль**:
   - Space.kt - модель пространства:
     ```kotlin
     data class Space(
         val id: Long,
         val name: String,
         val slug: String,
         val description: String?,
         val logo: String?,
         val type: SpaceType,
         val ownerId: Long,
         val membersCount: Int,
         val createdAt: Instant
     )

     enum class SpaceType { PUBLIC, PRIVATE }
     enum class SpaceMemberRole { OWNER, ADMIN, MEMBER }
     ```

   - SpaceDto.kt - DTO для Space API:
     ```kotlin
     data class CreateSpaceRequest(
         val name: String,
         val slug: String,
         val description: String?,
         val type: SpaceType,
         val inviteCode: String? = null
     )

     data class JoinSpaceRequest(
         val inviteCode: String? = null
     )

     data class SpaceResponse(
         val space: Space,
         val owner: UserPublicInfo,
         val currentUserRole: SpaceMemberRole?
     )

     data class SpaceListResponse(
         val spaces: List<SpaceResponse>,
         val total: Long
     )

     data class SpaceMemberResponse(
         val id: Long,
         val user: UserPublicInfo,
         val role: SpaceMemberRole,
         val joinedAt: Instant
     )
     ```

#### Этап 17: Backend - Space API endpoints (3-5 дней)
**Цель:** Реализовать CRUD API для пространств

1. **SpaceRepository** - слой работы с БД:
   - createSpace() - создание пространства (только авторизованные)
   - findById() - поиск пространства по ID
   - findBySlug() - поиск по slug (для URL)
   - getAllSpaces() - список всех публичных пространств + пространства пользователя
   - getMySpaces() - пространства текущего пользователя
   - updateSpace() - обновление (только OWNER/ADMIN)
   - deleteSpace() - удаление (только OWNER)
   - addMember() - добавление участника
   - removeMember() - удаление участника (только ADMIN/OWNER)
   - updateMemberRole() - изменение роли (только OWNER)
   - getMembers() - список участников пространства
   - isSlugUnique() - проверка уникальности slug
   - generateInviteCode() - генерация уникального кода приглашения

2. **SpaceService** - бизнес-логика:
   - Валидация: название (1-100 символов), slug (a-z0-9-), уникальность slug
   - Проверка прав доступа (только OWNER может удалять пространство)
   - Автоматическое добавление создателя как OWNER в SpaceMembers
   - Валидация inviteCode при присоединении к приватному пространству

3. **SpaceRoutes** - API endpoints:
   - `POST /api/spaces` - создание пространства (требует JWT)
   - `GET /api/spaces` - список доступных пространств (публичные + мои)
   - `GET /api/spaces/{id}` - детали пространства
   - `GET /api/spaces/my` - мои пространства (требует JWT)
   - `PUT /api/spaces/{id}` - обновление пространства (требует JWT, только OWNER/ADMIN)
   - `DELETE /api/spaces/{id}` - удаление пространства (требует JWT, только OWNER)
   - `POST /api/spaces/{id}/join` - присоединиться к пространству (требует JWT, для приватных нужен inviteCode)
   - `DELETE /api/spaces/{id}/leave` - покинуть пространство (требует JWT)
   - `GET /api/spaces/{id}/members` - список участников
   - `PUT /api/spaces/{id}/members/{userId}` - изменить роль участника (требует JWT, только OWNER)
   - `DELETE /api/spaces/{id}/members/{userId}` - удалить участника (требует JWT, только ADMIN/OWNER)
   - `POST /api/spaces/{id}/regenerate-invite` - перегенерировать код приглашения (требует JWT, только OWNER)

4. **Обновление Products/Services API**:
   - **Фильтрация по spaceId** (обязательный query параметр):
     - `GET /api/products?spaceId={spaceId}` - товары конкретного пространства
     - `GET /api/services?spaceId={spaceId}` - услуги конкретного пространства
   - **Создание с spaceId**:
     - `POST /api/products` - добавить spaceId в CreateProductRequest
     - `POST /api/services` - добавить spaceId в CreateServiceRequest
   - **Проверка доступа**: пользователь может создавать товары/услуги только в пространствах, где он участник

#### Этап 18: Mobile - Onboarding и выбор пространства (5-7 дней)
**Цель:** Добавить стартовый экран и выбор пространства перед входом в основное приложение

1. **OnboardingScreen** - стартовый экран приложения:
   - Логотип "Пульсар" с красивой анимацией
   - Слоган "В ритме твоего пространства"
   - Кнопка "Войти"
   - Кнопка "Регистрация"
   - Современный дизайн с градиентами

2. **SpaceRepository (Mobile)** - репозиторий для работы с Space API:
   - getMySpaces() - мои пространства
   - getAllSpaces() - публичные пространства
   - getSpaceById() - детали пространства
   - createSpace() - создание нового пространства
   - joinSpace() - присоединение к пространству
   - leaveSpace() - покинуть пространство

3. **SpaceSelectionViewModel** - управление состоянием выбора пространства:
   - Состояния: Loading, Success(mySpaces, publicSpaces), Empty, Error
   - loadSpaces() - загрузка доступных пространств
   - selectSpace(spaceId) - выбор пространства (сохранение в Settings)
   - joinSpace(inviteCode) - присоединение по коду

4. **SpaceSelectionScreen** - экран выбора пространства:
   - Показывается **ПОСЛЕ** успешной авторизации
   - Список "Мои пространства" (LazyColumn)
   - Список "Публичные пространства" (можно присоединиться)
   - Кнопка "Присоединиться по коду приглашения"
   - Кнопка "Создать новое пространство"
   - При клике на пространство → навигация на Main экран с этим spaceId

5. **CreateSpaceViewModel** - управление созданием пространства:
   - Состояния: Loading, Form, Creating, Success(spaceId), Error
   - SpaceFormState - состояние формы (name, slug, description, type, inviteCode, logo)
   - validateSlug() - валидация slug (a-z0-9-)
   - createSpace() - создание пространства

6. **CreateSpaceScreen** - форма создания пространства:
   - Поля: название, slug (автогенерация из названия), описание, логотип
   - RadioButton выбор типа: Публичное / Приватное
   - Если приватное - генерация inviteCode
   - Валидация slug (только строчные буквы, цифры, дефисы)
   - Кнопка "Создать пространство"

7. **JoinSpaceScreen** - присоединение по коду:
   - OutlinedTextField для ввода кода приглашения
   - Кнопка "Присоединиться"
   - Обработка ошибок (неверный код, уже участник и т.д.)

8. **Навигация (RootComponent обновлен)**:
   - Config: Login → Register → **SpaceSelection** → Main
   - После успешного login/register: навигация на SpaceSelection
   - После выбора пространства: навигация на Main с передачей spaceId
   - Хранение выбранного spaceId в Multiplatform Settings

#### Этап 19: Mobile - Интеграция Spaces в основное приложение (5-7 дней)
**Цель:** Добавить Space Context во все экраны, обновить фильтрацию товаров/услуг

1. **SpaceManager (expect/actual)** - управление текущим пространством:
   - getCurrentSpaceId() - получить текущее spaceId из Settings
   - setCurrentSpaceId(spaceId) - установить текущее spaceId
   - getCurrentSpace() - загрузить детали текущего пространства с сервера
   - clearCurrentSpace() - очистка при выходе

2. **MainComponent обновлен** - добавление Space Context:
   - Загрузка currentSpace при инициализации
   - Передача spaceId во все Child компоненты
   - Возможность переключения пространства (TODO: следующий этап)

3. **TopAppBar с Space Info** - отображение текущего пространства:
   - Логотип текущего пространства (маленький)
   - Название текущего пространства
   - Dropdown меню для переключения между пространствами (TODO: следующий этап)

4. **Обновление ProductListViewModel / ServiceListViewModel**:
   - Добавить spaceId в ProductFilters / ServiceFilters
   - Автоматическая фильтрация по текущему spaceId
   - Обновление при смене пространства

5. **Обновление CreateProductViewModel / CreateServiceViewModel**:
   - Автоматическое добавление spaceId при создании товара/услуги
   - Проверка: пользователь является участником пространства

6. **SpaceInfoScreen** - детали пространства:
   - Информация о пространстве (название, описание, логотип)
   - Роль текущего пользователя (OWNER, ADMIN, MEMBER)
   - Список участников (с пагинацией)
   - Кнопка "Покинуть пространство" (если не OWNER)
   - Настройки (для OWNER/ADMIN):
     - Редактирование названия, описания, логотипа
     - Управление участниками (изменение ролей, удаление)
     - Перегенерация кода приглашения (для приватных)

7. **SpaceSwitcherScreen** - переключение между пространствами:
   - Список всех доступных пространств пользователя
   - Текущее пространство помечено галочкой
   - При клике на другое пространство: смена контекста, перезагрузка данных

---

### **🟡 ПРИОРИТЕТ 2: Фаза UI/UX улучшения (1 неделя)**

#### Этап 20: Тёмная тема (3-4 дня)
**Цель:** Добавить поддержку тёмной темы и переключение

1. **ThemeManager (expect/actual)** - управление темой:
   - Enum ThemeMode { LIGHT, DARK, SYSTEM }
   - getCurrentTheme() - получить текущую тему из Settings
   - setTheme(mode) - установить тему
   - observeSystemTheme() - отслеживание системной темы (для SYSTEM mode)

2. **AppTheme обновлен** - Material Design 3 темы:
   - **darkColorScheme** - тёмная палитра:
     - primary, secondary, tertiary
     - background, surface, surfaceVariant
     - onPrimary, onSecondary, onBackground, onSurface
   - **lightColorScheme** - обновленная светлая палитра (красивее)
   - isSystemInDarkTheme() - определение системной темы

3. **ThemeToggleButton** UI компонент:
   - IconButton в TopAppBar или Settings
   - Иконка: Солнце (светлая) / Луна (тёмная) / Авто (системная)
   - DropdownMenu с выбором: Светлая / Тёмная / Системная
   - Анимация смены темы (fade transition)

4. **Интеграция в RootContent**:
   - themeManager инжектится через Koin
   - observeAsState() для отслеживания изменений темы
   - Передача isDarkTheme в AppTheme

5. **Обновление компонентов под тёмную тему**:
   - Проверка всех экранов на читаемость в тёмной теме
   - Корректировка цветов для контраста

#### Этап 21: Улучшение светлой темы (2-3 дня)
**Цель:** Сделать светлую тему более современной и привлекательной

1. **Дизайн-система**:
   - Использовать Material 3 Theme Builder для подбора палитры
   - Красивые цвета: основной цвет (primary), акцентный (secondary)
   - Градиенты для TopAppBar или важных элементов
   - Скругленные углы (rounded corners) для Card компонентов

2. **Обновление компонентов**:
   - **ProductCard / ServiceCard**: более современный дизайн
     - Градиент для фона цены
     - Elevation и тени
     - Иконки категорий цветные
   - **TopAppBar**: градиентный фон или акцентный цвет
   - **FloatingActionButton**: анимация при наведении (ripple effect)
   - **BottomNavigation**: Material 3 стиль с индикаторами

3. **Типографика**:
   - Использовать Material 3 Typography (displayLarge, bodyMedium и т.д.)
   - Красивые шрифты (например, Roboto, Montserrat)

---

### **🟢 ПРИОРИТЕТ 3: Фаза Аутентификация (3-5 дней)**

#### Этап 22: Вход по email (3-5 дней)
**Цель:** Добавить альтернативный способ входа через email

1. **Backend обновлен**:
   - POST /api/auth/login - поддержка email вместо phone:
     ```kotlin
     data class LoginRequest(
         val phoneOrEmail: String, // может быть телефон или email
         val password: String
     )
     ```
   - Валидация: если содержит "@" - это email, иначе - телефон
   - POST /api/auth/register - email обязателен (уже реализовано)

2. **LoginScreen обновлен**:
   - RadioButton выбор способа входа:
     - "По номеру телефона"
     - "По email"
   - OutlinedTextField меняет placeholder и валидацию в зависимости от выбора
   - Валидация email: EmailValidator.isValid()
   - Валидация телефона: PhoneValidator.isValid()

3. **LoginViewModel обновлен**:
   - LoginMode enum: PHONE, EMAIL
   - updateLoginMode(mode) - смена режима входа
   - Валидация в зависимости от режима

---

### **🔵 ПРИОРИТЕТ 4: Фаза Социальные функции (2-3 недели)**

#### Этап 23: Лента новостей (News Feed) (5-7 дней)
**Цель:** Добавить ленту новостей для каждого пространства

1. **Backend**:
   - Таблица `SpaceNews`:
     - id: Long (PK)
     - spaceId: Long (FK to Spaces)
     - authorId: Long (FK to Users) - автор новости
     - title: String - заголовок новости
     - content: String - текст новости
     - images: List<String> - изображения (опциональные)
     - createdAt: Instant
     - updatedAt: Instant

   - NewsRepository, NewsService, NewsRoutes

   - API endpoints:
     - `GET /api/spaces/{spaceId}/news` - список новостей пространства (пагинация)
     - `POST /api/spaces/{spaceId}/news` - создание новости (требует JWT, только ADMIN/OWNER)
     - `GET /api/spaces/{spaceId}/news/{id}` - детали новости
     - `PUT /api/spaces/{spaceId}/news/{id}` - обновление новости (требует JWT, только автор или ADMIN)
     - `DELETE /api/spaces/{spaceId}/news/{id}` - удаление новости (требует JWT, только автор или ADMIN)

2. **Mobile**:
   - NewsRepository - слой работы с News API

   - **NewsListViewModel** - управление списком новостей:
     - NewsPagingSource с Paging 3
     - Фильтрация по spaceId
     - Pull-to-refresh

   - **NewsListScreen** - лента новостей:
     - LazyColumn с карточками новостей
     - NewsCard - карточка новости (заголовок, превью контента, автор, дата)
     - FloatingActionButton "+" для создания новости (только ADMIN/OWNER)
     - Клик на новость → NewsDetailScreen

   - **NewsDetailScreen** - детали новости:
     - Полный текст новости
     - Галерея изображений (если есть)
     - Информация об авторе
     - Кнопка "Редактировать" (если автор или ADMIN)
     - Кнопка "Удалить" (если автор или ADMIN)

   - **CreateNewsScreen** - форма создания новости:
     - OutlinedTextField для заголовка
     - OutlinedTextField (multiline) для текста
     - ImagePicker для добавления изображений (до 5)
     - Кнопка "Опубликовать"

   - **Навигация**:
     - Добавить вкладку "Новости" в Bottom Navigation (4 вкладки: Товары, Услуги, Новости, Профиль)
     - Config.NewsList, Config.NewsDetail, Config.CreateNews

#### Этап 24: Раздел документации (Documents) (3-5 дней)
**Цель:** Добавить возможность загружать и просматривать документы (устав, правила и т.д.)

1. **Backend**:
   - Таблица `SpaceDocuments`:
     - id: Long (PK)
     - spaceId: Long (FK to Spaces)
     - title: String - название документа
     - description: String? - описание
     - fileUrl: String - URL файла (PDF, DOCX и т.д.)
     - fileType: String - MIME type (application/pdf, application/vnd.openxmlformats-officedocument.wordprocessingml.document)
     - fileSize: Long - размер файла в байтах
     - uploadedBy: Long (FK to Users)
     - createdAt: Instant

   - DocumentRepository, DocumentService, DocumentRoutes

   - API endpoints:
     - `GET /api/spaces/{spaceId}/documents` - список документов пространства
     - `POST /api/spaces/{spaceId}/documents` - загрузка документа (требует JWT, только ADMIN/OWNER)
     - `GET /api/spaces/{spaceId}/documents/{id}` - детали документа
     - `DELETE /api/spaces/{spaceId}/documents/{id}` - удаление документа (требует JWT, только ADMIN/OWNER)

   - FileRoutes обновлен:
     - Поддержка загрузки PDF, DOCX файлов
     - Валидация MIME type
     - Максимальный размер файла: 10 MB

2. **Mobile**:
   - DocumentRepository - слой работы с Documents API

   - **DocumentsListViewModel** - управление списком документов:
     - Состояния: Loading, Success(documents), Empty, Error
     - loadDocuments() - загрузка документов пространства
     - deleteDocument(id) - удаление (только ADMIN/OWNER)

   - **DocumentsListScreen** - список документов:
     - LazyColumn с карточками документов
     - DocumentCard - карточка документа (название, описание, тип файла, размер, дата)
     - FloatingActionButton "+" для загрузки документа (только ADMIN/OWNER)
     - Клик на документ → открытие в браузере или скачивание

   - **UploadDocumentScreen** - загрузка документа:
     - FilePicker для выбора файла (PDF, DOCX)
     - OutlinedTextField для названия и описания
     - Кнопка "Загрузить"

   - **Навигация**:
     - Добавить в Navigation Drawer (боковое меню) пункт "Документы"
     - Или добавить в ProfileScreen кнопку "Документы пространства"
     - Config.DocumentsList, Config.UploadDocument

#### Этап 25: Чат между пользователями (Messaging) (7-10 дней)
**Цель:** Добавить возможность переписки между пользователями внутри пространства

1. **Backend**:
   - Таблицы:
     - `Conversations`:
       - id: Long (PK)
       - spaceId: Long (FK to Spaces) - чаты привязаны к пространству
       - participant1Id: Long (FK to Users)
       - participant2Id: Long (FK to Users)
       - createdAt: Instant
       - lastMessageAt: Instant
       - Unique index: (spaceId, participant1Id, participant2Id)

     - `Messages`:
       - id: Long (PK)
       - conversationId: Long (FK to Conversations)
       - senderId: Long (FK to Users)
       - text: String - текст сообщения
       - createdAt: Instant
       - isRead: Boolean - прочитано ли сообщение

   - ConversationRepository, MessageRepository, ChatService

   - **WebSocket для real-time сообщений** (Ktor WebSocket plugin):
     - WS endpoint: `/api/chat/{conversationId}`
     - Аутентификация через JWT (query параметр или header)
     - Отправка сообщений через WebSocket
     - Получение новых сообщений в реальном времени

   - REST API endpoints:
     - `GET /api/conversations` - список чатов текущего пользователя (пагинация)
     - `GET /api/conversations/{id}/messages` - сообщения чата (пагинация)
     - `POST /api/conversations` - создание нового чата с пользователем (body: { userId: Long })
     - `POST /api/conversations/{id}/messages` - отправка сообщения (fallback для WebSocket)
     - `PUT /api/conversations/{id}/messages/{messageId}/read` - отметить сообщение как прочитанное

2. **Mobile**:
   - **WebSocket интеграция** (Ktor Client WebSocket):
     - ChatWebSocketClient - управление WebSocket соединением
     - Подключение к `/api/chat/{conversationId}`
     - Отправка сообщений через WS
     - Получение новых сообщений через Flow

   - ConversationRepository, MessageRepository

   - **ConversationsListViewModel** - управление списком чатов:
     - Состояния: Loading, Success(conversations), Empty, Error
     - loadConversations() - загрузка списка чатов
     - createConversation(userId) - создание нового чата

   - **ConversationsListScreen** - список чатов:
     - LazyColumn с карточками чатов
     - ConversationCard - карточка чата (имя собеседника, последнее сообщение, время, badge с непрочитанными)
     - Клик на чат → ChatScreen

   - **ChatViewModel** - управление перепиской:
     - Состояния: Loading, Success(messages), Error
     - loadMessages() - загрузка истории сообщений
     - sendMessage(text) - отправка сообщения через WebSocket
     - observeNewMessages() - подписка на новые сообщения через Flow
     - markAsRead(messageId) - отметить сообщение как прочитанное

   - **ChatScreen** - переписка с пользователем:
     - LazyColumn с сообщениями (обратный порядок - новые снизу)
     - MessageBubble - компонент для отображения сообщения (свои / чужие)
     - OutlinedTextField + IconButton для ввода и отправки сообщения
     - Автопрокрутка к последнему сообщению
     - Pull-to-refresh для загрузки истории
     - Индикатор "печатает..." (опционально)

   - **Push-уведомления** (опционально):
     - Firebase Cloud Messaging (FCM)
     - Backend отправляет push при новом сообщении
     - Клик на уведомление → открытие чата

   - **Навигация**:
     - Добавить иконку "Сообщения" в TopAppBar (с badge непрочитанных)
     - Или добавить вкладку "Чаты" в Bottom Navigation (5 вкладок)
     - Config.ConversationsList, Config.Chat(conversationId)

---

### **🟣 ПРИОРИТЕТ 5: Фаза Финализация MVP (1-2 недели)**

#### Этап 26: Редактирование профиля (3-4 дня)
**Цель:** Добавить возможность редактирования профиля и загрузки аватара

1. **EditProfileViewModel** - управление редактированием профиля:
   - Состояния: Loading, Form, Updating, Success, Error
   - ProfileFormState - состояние формы (name, email, bio, avatar)
   - loadProfile() - загрузка текущего профиля
   - updateProfile() - обновление профиля
   - uploadAvatar(image) - загрузка аватара

2. **EditProfileScreen** - форма редактирования:
   - OutlinedTextField для имени
   - OutlinedTextField для email
   - OutlinedTextField (multiline) для биографии
   - ImagePicker для аватара (Camera/Gallery)
   - Предпросмотр аватара (круглый)
   - Кнопка "Сохранить изменения"

3. **Навигация**:
   - ProfileScreen: кнопка "Редактировать профиль" → EditProfileScreen
   - Config.EditProfile

#### Этап 27: Избранное (Favorites) (3-4 дня)
**Цель:** Добавить экран избранных товаров

1. **FavoritesViewModel** - управление избранным:
   - FavoritesPagingSource с Paging 3
   - Фильтрация по текущему spaceId
   - Pull-to-refresh

2. **FavoritesScreen** - список избранных товаров:
   - LazyColumn с ProductCard
   - Обработка пустого состояния ("Нет избранных товаров")

3. **Навигация**:
   - Добавить в ProfileScreen кнопку "Избранное"
   - Или иконку в TopAppBar
   - Config.Favorites

#### Этап 28: Поиск и фильтры (3-5 дней)
**Цель:** Добавить глобальный поиск и расширенные фильтры

1. **SearchViewModel** - управление поиском:
   - Состояния: Idle, Searching, Success(products, services), Empty, Error
   - search(query) - поиск товаров и услуг одновременно
   - Фильтры: категория, диапазон цен, состояние (для товаров)

2. **SearchScreen** - экран поиска:
   - SearchBar (Material 3)
   - Tabs: "Товары" / "Услуги" / "Все"
   - LazyColumn с результатами
   - Кнопка "Фильтры" → FilterScreen

3. **FilterScreen** - расширенные фильтры:
   - Выбор категории (Dropdown)
   - RangeSlider для диапазона цен
   - Checkbox для состояния (Новое/Б/У)
   - Кнопка "Применить"

4. **Навигация**:
   - Добавить иконку поиска в TopAppBar
   - Config.Search, Config.Filter

---

### **⚪ Этап 29: Тестирование и деплой (1 неделя)**

1. **Тестирование**:
   - Функциональное тестирование всех фич
   - Тестирование на разных устройствах
   - Проверка UI в светлой и тёмной теме
   - Тестирование переключения между пространствами
   - Стресс-тестирование чата (WebSocket)

2. **Оптимизация**:
   - Проверка производительности (Profiler)
   - Оптимизация размера APK
   - Минификация кода (ProGuard)

3. **Backend Production**:
   - Переменные окружения (JWT_SECRET, DATABASE_URL)
   - Rate Limiting middleware
   - CORS для specific origins
   - Graceful Shutdown
   - Health Check с проверкой БД

4. **Деплой Backend**:
   - Деплой на VPS (DigitalOcean, AWS, etc.)
   - Настройка домена и SSL (Let's Encrypt)
   - Настройка PostgreSQL
   - Nginx reverse proxy

5. **Публикация Android**:
   - Подготовка APK/AAB
   - Создание store listing (Google Play Console)
   - Скриншоты, описание, иконка
   - Бета-тестирование (Internal Testing → Closed Testing)
   - Публикация в Google Play

---

## 📊 Итоговая структура проекта "Пульсар"

### Backend (Kotlin + Ktor):
```
backend/
├── tables/
│   ├── Users.kt
│   ├── Spaces.kt ✨ NEW
│   ├── SpaceMembers.kt ✨ NEW
│   ├── Products.kt (+ spaceId FK)
│   ├── Services.kt (+ spaceId FK)
│   ├── SpaceNews.kt ✨ NEW
│   ├── SpaceDocuments.kt ✨ NEW
│   ├── Conversations.kt ✨ NEW
│   └── Messages.kt ✨ NEW
├── repository/
│   ├── SpaceRepository.kt ✨ NEW
│   ├── NewsRepository.kt ✨ NEW
│   ├── DocumentRepository.kt ✨ NEW
│   ├── ConversationRepository.kt ✨ NEW
│   └── MessageRepository.kt ✨ NEW
├── routes/
│   ├── SpaceRoutes.kt ✨ NEW
│   ├── NewsRoutes.kt ✨ NEW
│   ├── DocumentRoutes.kt ✨ NEW
│   ├── ChatRoutes.kt ✨ NEW (WebSocket)
│   └── ...
└── plugins/
    └── WebSockets.kt ✨ NEW
```

### Mobile (Kotlin Multiplatform + Compose):
```
sharedUI/
├── presentation/screens/
│   ├── onboarding/ ✨ NEW
│   │   └── OnboardingScreen.kt
│   ├── spaces/ ✨ NEW
│   │   ├── SpaceSelectionScreen.kt
│   │   ├── CreateSpaceScreen.kt
│   │   ├── JoinSpaceScreen.kt
│   │   ├── SpaceInfoScreen.kt
│   │   └── SpaceSwitcherScreen.kt
│   ├── news/ ✨ NEW
│   │   ├── NewsListScreen.kt
│   │   ├── NewsDetailScreen.kt
│   │   └── CreateNewsScreen.kt
│   ├── documents/ ✨ NEW
│   │   ├── DocumentsListScreen.kt
│   │   └── UploadDocumentScreen.kt
│   ├── chat/ ✨ NEW
│   │   ├── ConversationsListScreen.kt
│   │   └── ChatScreen.kt
│   ├── search/ ✨ NEW
│   │   ├── SearchScreen.kt
│   │   └── FilterScreen.kt
│   └── ...
├── utils/
│   ├── ThemeManager.kt ✨ NEW
│   ├── SpaceManager.kt ✨ NEW
│   └── ChatWebSocketClient.kt ✨ NEW
└── theme/
    ├── AppTheme.kt (обновлен с darkColorScheme)
    └── ...
```

---

## 🎯 Следующий приоритетный шаг (СЕЙЧАС):

### **Этап 18: Mobile — Onboarding и выбор пространства**

**Статус:** 🟢 В процессе — на стороне клиента появились `SpaceManager`, `SpaceRepository`, экран/компонент SpaceSelection и обновлённый `RootComponent`, который ведёт пользователя в выбор пространства после логина/регистрации. Профиль получил кнопку «Сменить пространство», а все product/service запросы автоматически добавляют `spaceId`.

**Свежие результаты:**
- `SpaceManager` хранит выбранное пространство в Settings; `ApiClient` автоматически прокидывает `spaceId` во все маршруты (списки, CRUD, избранное).
- Новый экран SpaceSelection (Compose + Decompose компонент) поддерживает просмотр публичных пространств, join по invite-коду и создание собственного пространства; после выбора мы переключаемся в `MainComponent`.
- Логин/регистрация теперь передают `defaultSpaceId`, Root роутит `Login → SpaceSelection → Main` и очищает `SpaceManager` при logout/switch.
- SpaceSelection теперь разделяет «Мои пространства» (по текущему членству) и «Доступные пространства», позволяет выбирать уже существующий Space без повторных запросов и валидирует slug (автогенерация из названия, подсказки/ошибки для пользователя).
- Обновили цветовую схему (новая светлая/тёмная палитры), добавили `ThemeManager` + экран «Настройки» в профиле: пользователь может переключать тему (системная/светлая/тёмная), выбор сохраняется в Settings и применяется на уровне всего приложения.

**Оставшиеся задачи Этапа 18:**
1. Улучшить UX отображения и переключения текущего пространства (баннер/название в `Main`/`Profile`, быстрый вход в SpaceSelection из тулбара).
2. После починки Gradle-проблем прогнать `:sharedUI:allTests` + ручной smoke Mobile↔Backend, чтобы проверить новый onboarding.
3. Подготовить UI hooks для будущего переключателя пространств (Этап 19) внутри основного приложения, чтобы смена Space была доступна не только из профиля.

**Почему это критично:**
- Без принудительного выбора пространства мобильный клиент не сможет использовать backend, который теперь жёстко фильтрует данные по `spaceId`.
- Space-aware onboarding — база перед внедрением Space-specific экранов (новости, документы, чат).
- Это минимизирует риск случайного доступа к чужим данным и открывает путь к многопространственной монетизации.

---

**Версия документа**: 3.2
**Дата создания**: 2025-11-09
**Последнее обновление**: 2025-11-16 (SpaceSelection разделён на блоки «Мои/Доступные пространства», добавлена slug-валидация, обновлена цветовая схема + экран настроек темы.)
**Автор**: Claude AI + Team
**Ребрендинг**: SocialSpace → **Пульсар** ("В ритме твоего пространства")
