# 🚀 Быстрый старт SocialSpace

## Проблема: бесконечный лоадер на экране товаров

**Причина**: Backend сервер не запущен или в базе данных нет тестовых данных.

**Решение**: Запустить backend и добавить тестовые данные.

---

## Шаг 1: Запуск Backend сервера

### Вариант А: Через IntelliJ IDEA (рекомендуется)

1. Откройте проект в IntelliJ IDEA
2. Найдите файл `backend/src/main/kotlin/info/javaway/sc/backend/Application.kt`
3. Нажмите на зелёную кнопку ▶️ рядом с функцией `main()`
4. Дождитесь запуска сервера (в консоли должно появиться `Application started`)

### Вариант Б: Через командную строку

```bash
# Из корня проекта
./gradlew :backend:run
```

### Вариант В: Через Gradle из IDE

1. Откройте Gradle панель в IDEA
2. Найдите `SocialSpace > backend > Tasks > application > run`
3. Запустите задачу

---

## Шаг 2: Проверка работы Backend

Откройте браузер и перейдите по адресу:
```
http://localhost:8080/health
```

Если видите `{"status":"ok"}`, значит backend запущен успешно! ✅

---

## Шаг 3: Добавление тестовых данных

### Автоматически (через скрипт)

```bash
cd backend
./add_test_data.sh
```

Скрипт автоматически создаст:
- 2 тестовых пользователя
- 6 товаров (диван, ноутбук, велосипед, холодильник, коляска, iPhone)

### Тестовые аккаунты

**Пользователь 1:**
- Телефон: `+79991234567`
- Пароль: `password123`

**Пользователь 2:**
- Телефон: `+79997654321`
- Пароль: `password123`

---

## Шаг 4: Запуск Mobile приложения

1. Убедитесь, что backend запущен (Шаг 1-2)
2. Запустите Android приложение:
   ```bash
   ./gradlew :androidApp:installDebug
   ```
3. Или запустите из Android Studio:
   - Откройте `androidApp` модуль
   - Нажмите Run ▶️

---

## Проверка списка товаров

После входа в приложение:
1. Войдите с тестовым аккаунтом (`+79991234567` / `password123`)
2. Перейдите на вкладку "Товары" (нижняя навигация)
3. Вы должны увидеть список из 6 товаров

**Если видите бесконечный лоадер:**
- Проверьте, что backend запущен (`http://localhost:8080/health`)
- Проверьте логи приложения в Logcat (Android Studio)
- Проверьте, что в базе есть данные (запустите скрипт из Шага 3)

---

## Возможные проблемы и решения

### Backend не запускается

**Ошибка**: `Port 8080 is already in use`
```bash
# Найдите процесс на порту 8080
lsof -ti:8080
# Убейте процесс
kill -9 <PID>
```

**Ошибка**: `Could not resolve dependencies`
- Проверьте подключение к интернету
- Запустите через IntelliJ IDEA (у неё есть кеш зависимостей)

### Mobile приложение не может подключиться к Backend

**Для Android Emulator:**
- Backend должен быть доступен на `http://10.0.2.2:8080`
- Проверьте, что backend запущен на порту 8080

**Для реального устройства:**
- Замените в `ApiClient.kt` URL на IP вашего компьютера:
  ```kotlin
  private val baseUrl: String = "http://192.168.1.100:8080" // Ваш IP
  ```

### Список товаров пуст

- Запустите скрипт добавления данных: `./backend/add_test_data.sh`
- Или создайте товар вручную через приложение

---

## Полезные команды

```bash
# Проверить список товаров через API
curl http://localhost:8080/api/products

# Проверить категории
curl http://localhost:8080/api/categories

# Зарегистрировать нового пользователя
curl -X POST http://localhost:8080/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"phone":"+79001234567","name":"Тест","password":"test123"}'
```

---

## Следующие шаги

После успешного запуска можно приступать к разработке:
- ✅ Этап 9 завершён: список товаров работает
- ⏳ Этап 10: детальная карточка товара и профиль
- ⏳ Этап 11: создание объявлений
- ⏳ Этап 12: категории и услуги

---

**Документация проекта**: `CLAUDE.md`
**API документация**: `backend/API_DOCUMENTATION.md`
