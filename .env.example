# Database Configuration
# Для локальной разработки используется H2 (не требует настройки)
# Для production раскомментируйте и настройте PostgreSQL

# PostgreSQL (Production)
# DB_DRIVER=org.postgresql.Driver
# DB_URL=jdbc:postgresql://localhost:5432/socialspace
# DB_USER=postgres
# DB_PASSWORD=postgres

# JWT Configuration
JWT_SECRET=your-secret-key-change-this-in-production
JWT_ISSUER=socialspace
JWT_AUDIENCE=socialspace-users
JWT_REALM=SocialSpace API
JWT_EXPIRATION=3600000

# Server Configuration
PORT=8080
